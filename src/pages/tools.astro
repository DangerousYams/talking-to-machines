---
import '../styles/global.css';
import ToolWall from '../components/widgets/ch4/ToolWall';
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content="Browse 30+ AI tools across image generation, video, music, research, coding, and more. Filterable, searchable, with details on each." />
    <meta property="og:title" content="AI Tool Directory — Talking to Machines" />
    <meta property="og:description" content="Browse 30+ AI tools across 8 categories. Find the right tool for the job." />
    <title>AI Tool Directory — Talking to Machines</title>
  </head>
  <body class="bg-cream text-deep min-h-screen">

    <!-- ═══ NAV BAR ═══ -->
    <nav style="padding: 1rem 1.5rem; display: flex; align-items: center; justify-content: space-between; max-width: 1000px; margin: 0 auto;">
      <a
        href="/"
        style="display: inline-flex; align-items: center; gap: 6px; font-family: var(--font-mono); font-size: 0.75rem; font-weight: 600; letter-spacing: 0.04em; color: var(--color-subtle); text-decoration: none; opacity: 0.6; transition: opacity 0.2s;"
        onmouseenter="this.style.opacity='1'"
        onmouseleave="this.style.opacity='0.6'"
      >
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="15 18 9 12 15 6" />
        </svg>
        Home
      </a>
      <a
        href="/ch4"
        style="font-family: var(--font-mono); font-size: 0.7rem; font-weight: 500; letter-spacing: 0.04em; color: var(--color-subtle); text-decoration: none; opacity: 0.5; transition: opacity 0.2s;"
        onmouseenter="this.style.opacity='0.8'"
        onmouseleave="this.style.opacity='0.5'"
      >
        From Ch4: The AI Landscape
      </a>
    </nav>

    <!-- ═══ HERO ═══ -->
    <header style="max-width: 700px; margin: 0 auto; padding: 2rem 1.5rem 2.5rem; text-align: center;">
      <!-- Icon -->
      <div style="display: inline-flex; align-items: center; justify-content: center; width: 48px; height: 48px; border-radius: 14px; background: linear-gradient(135deg, #0EA5E9, #7B61FF); margin-bottom: 1.25rem;">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <rect x="3" y="3" width="7" height="7" />
          <rect x="14" y="3" width="7" height="7" />
          <rect x="14" y="14" width="7" height="7" />
          <rect x="3" y="14" width="7" height="7" />
        </svg>
      </div>

      <h1 style="font-family: var(--font-heading); font-size: clamp(2rem, 5vw, 3rem); font-weight: 800; color: var(--color-deep); margin: 0 0 0.75rem; line-height: 1.1;">
        AI Tool Directory
      </h1>
      <p style="font-family: var(--font-body); font-size: clamp(1rem, 2.5vw, 1.15rem); color: var(--color-subtle); line-height: 1.6; margin: 0; max-width: 480px; display: inline-block;">
        The landscape moves fast. Here's what's out there right now — filterable by category and searchable by name.
      </p>
    </header>

    <!-- ═══ TOOL WALL ═══ -->
    <main style="max-width: 900px; margin: 0 auto; padding: 0 1rem 4rem;">
      <ToolWall client:load />
    </main>

    <!-- ═══ FOOTER ═══ -->
    <footer style="padding: 3rem 1.5rem; text-align: center; border-top: 1px solid rgba(26,26,46,0.06);">
      <p style="font-family: var(--font-body); font-size: 0.9rem; color: var(--color-subtle); margin: 0 0 0.5rem;">
        Want the full story behind these tools?
      </p>
      <a
        href="/ch4"
        style="display: inline-flex; align-items: center; gap: 6px; font-family: var(--font-heading); font-size: 0.95rem; font-weight: 700; color: #0EA5E9; text-decoration: none; transition: opacity 0.2s;"
        onmouseenter="this.style.opacity='0.7'"
        onmouseleave="this.style.opacity='1'"
      >
        Read Chapter 4: The AI Landscape
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="9 18 15 12 9 6" />
        </svg>
      </a>
    </footer>
  </body>
</html>
