---
export const prerender = true;
import ChapterLayout from '../../layouts/ChapterLayout.astro';
import { chapters } from '../../data/chapters';
import FadeInBlock from '../../components/scroll/FadeInBlock';
import AccentBar from '../../components/ui/AccentBar';
import Divider from '../../components/ui/Divider';
import PromptMakeover from '../../components/widgets/ch1/PromptMakeover';
import PromptRoast from '../../components/widgets/ch1/PromptRoast';
import BreakReveal from '../../components/breaks/BreakReveal';
import SharePrompt from '../../components/ui/SharePrompt';

const chapter = chapters[0];
---

<ChapterLayout chapter={chapter} locale="gu">

  <!-- ═══ HERO ═══ -->
  <section class="min-h-[80vh] flex flex-col items-center justify-end px-6 pb-20 relative overflow-hidden">
    <div class="chapter-number" style={`color: ${chapter.accent};`}>1</div>

    <div class="max-w-[680px] mx-auto text-center relative z-10">
      <FadeInBlock client:visible>
        <p class="text-[13px] italic text-subtle/60 mb-5" style="font-family: var(--font-body);">
          પ્રકરણ એક
        </p>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.15}>
        <h1
          class="mb-8 leading-[1.08]"
          style={`font-family: var(--font-heading); color: ${chapter.accent}; font-size: clamp(2.8rem, 7vw, 4.5rem); font-weight: 800; letter-spacing: -0.03em;`}
        >
          તું પહેલેથી જ<br />AI બોલે છે
        </h1>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.3}>
        <p class="text-lg md:text-xl italic text-deep/40 leading-relaxed max-w-sm mx-auto" style="font-family: var(--font-body);">
          તારો પહેલો draft magic નહીં હોય.<br />પણ ત્રીજો હોઈ શકે.
        </p>
      </FadeInBlock>
    </div>

    <div class="absolute bottom-6 left-1/2 -translate-x-1/2 opacity-20 animate-pulse">
      <div class="w-px h-10 bg-deep/40"></div>
    </div>
  </section>

  <!-- ═══ OPENING HOOK ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-24 pb-16">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] drop-cap" style="margin: 0;">
        તું આખી જિંદગી AI સાથે વાત કરતો આવ્યો છે — દરેક search query, દરેક Siri ને પૂછેલો સવાલ, દરેક Netflix recommendation. પણ machine <em>ને</em> કંઈક કહેવું અને machine <em>સાથે</em> વાત કરવી — એમાં ફરક છે. અત્યારે લાખો લોકો prompt ટાઈપ કરે, સાધારણ response મળે, અને વિચારે: "AI એટલું smart નથી લાગતું." એ ખોટા છે. AI શું કરી શકે અને મોટાભાગના લોકો એની પાસે શું કરાવે — આ gap ઘણો મોટો છે. અને આ technology ની problem નથી — આ communication ની problem છે.
      </p>
    </FadeInBlock>
  </section>

  <Divider accent={chapter.accent} client:load />

  <!-- ═══ THE FIVE BUILDING BLOCKS ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-16">
    <FadeInBlock client:visible>
      <AccentBar color={chapter.accent} className="w-16 mb-10" client:visible />
      <h2 class="mb-8" style="font-family: var(--font-heading);">
        પાંચ Building Blocks
      </h2>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] mb-14" style="margin-top: 0;">
        અહીં secret છે: AI માંથી અદ્ભુત results મેળવનારા લોકો તારાથી smart નથી. એ એક formula વાપરે છે — પાંચ building blocks જે દર વખતે કામ કરે છે. તારે હંમેશા પાંચેય ની જરૂર નથી. પણ એ exist કરે છે એ જાણવું — એ Matrix જોતા શીખવા જેવું છે.
      </p>
    </FadeInBlock>

    <div style="display: flex; flex-direction: column; gap: 2rem; margin-bottom: 3rem;">
      {[
        { letter: 'R', name: 'ભૂમિકા (Role)', color: '#E94560', text: 'AI ને કોણ બનવું જોઈએ? Tutor અલગ રીતે સમજાવે, professor અલગ રીતે. Role બધું બદલી નાખે.' },
        { letter: 'T', name: 'કાર્ય (Task)', color: '#0F3460', text: 'ચોક્કસ શું કરવાનું છે? "મને help કર" નહીં પણ "મારું thesis statement મજબૂત કર." એક prompt માં એક task.' },
        { letter: 'F', name: 'ફોર્મેટ (Format)', color: '#7B61FF', text: 'Response કેવો દેખાવો જોઈએ? "3 alternatives numbered list માં આપ." Format rambling અટકાવે.' },
        { letter: 'C', name: 'મર્યાદા (Constraints)', color: '#16C79A', text: 'શું ન કરવું? "Clichés ન વાપર. 200 words ની અંદર રાખ." Guardrails ખરાબ output અટકાવે.' },
        { letter: 'E', name: 'ઉદાહરણ (Examples)', color: '#F5A623', text: 'કહેવા કરતાં બતાવ. "સારું" કેવું દેખાય એનું એક example, સો words ના description કરતાં વધુ કામનું.' },
      ].map((block, i) => (
        <FadeInBlock client:visible delay={i * 0.08}>
          <div style={`display: flex; gap: 1.5rem; align-items: flex-start;`}>
            <div style={`flex-shrink: 0; width: 40px; height: 40px; border-radius: 10px; display: flex; align-items: center; justify-content: center; font-family: var(--font-heading); font-size: 1.1rem; font-weight: 800; color: white; background: ${block.color};`}>
              {block.letter}
            </div>
            <div style="flex: 1;">
              <p style={`font-family: var(--font-heading); font-size: 1.1rem; font-weight: 700; color: ${block.color}; margin: 0 0 0.35rem; line-height: 1.3;`}>
                {block.name}
              </p>
              <p style="font-size: 1rem; line-height: 1.75; color: var(--color-deep); opacity: 0.75; margin: 0;">
                {block.text}
              </p>
            </div>
          </div>
        </FadeInBlock>
      ))}
    </div>

    <FadeInBlock client:visible delay={0.5}>
      <div class="insight-box">
        <p style="font-family: var(--font-heading); font-size: 0.85rem; font-weight: 700; color: var(--color-amber); margin: 0 0 0.5rem;">મુખ્ય વાત</p>
        <p style="font-size: 1rem; line-height: 1.75; margin: 0;">
          તારે દર વખતે પાંચેય ની જરૂર નથી. પણ જ્યારે AI ગંદો output આપે, ત્યારે જો શું missing છે — લગભગ હંમેશા આ પાંચમાંથી કોઈ એક હશે.
        </p>
      </div>
    </FadeInBlock>
  </section>

  <!-- ═══ WIDGET BRIDGE ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-4 pb-8">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] text-center mx-auto italic text-deep/50" style="font-family: var(--font-body); text-wrap: balance;">
        બસ theory પૂરી. Building blocks toggle કર અને AI ના response ને real time માં બદલાતો જો.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ THE WIDGET ═══ -->
  <section id="prompt-makeover" class="max-w-[900px] mx-auto px-6 md:px-10 py-8">
    <PromptMakeover client:visible />
  </section>

  <Divider accent={chapter.accent} client:load />

  <SharePrompt text="હમણાં જ સારા AI prompt ના 5 building blocks શોધ્યા. આ interactive course ખરેખર સરસ છે — તું જાતે prompts બનાવે અને real time માં improve થતા જુએ." client:visible />

  <!-- ═══ SEND-OFF ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-24">
    <FadeInBlock client:visible>
      <div class="pull-quote" style="border-color: var(--color-navy); max-width: 50ch; margin: 0 auto 2rem;">
        તું પહેલેથી જ AI બોલે છે. બસ હવે ઈરાદાથી બોલ.
      </div>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] text-center mx-auto" style="max-width: 55ch;">
        આગળ: એવી techniques જે સાદા સવાલ-જવાબને ખરી conversation માં બદલે — જ્યાં AI ફક્ત જવાબ નથી આપતું, એ <em>તારી સાથે વિચારે છે</em>.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ BREAK ═══ -->
  <section id="break" class="py-16 px-6" style="background: linear-gradient(180deg, var(--color-cream) 0%, rgba(233,69,96,0.06) 15%, rgba(233,69,96,0.06) 85%, var(--color-cream) 100%);">
    <div class="max-w-[900px] mx-auto">
      <BreakReveal accentColor="#E94560" toolName="Prompt Roast" client:visible>
        <PromptRoast client:visible />
      </BreakReveal>
    </div>
  </section>

</ChapterLayout>
