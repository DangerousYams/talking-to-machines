---
export const prerender = true;
import ChapterLayout from '../../layouts/ChapterLayout.astro';
import { chapters } from '../../data/chapters';
import FadeInBlock from '../../components/scroll/FadeInBlock';
import AccentBar from '../../components/ui/AccentBar';
import Divider from '../../components/ui/Divider';
import ContextWindowViz from '../../components/widgets/ch3/ContextWindowViz';
import VibeCheck from '../../components/breaks/VibeCheck';
import BreakReveal from '../../components/breaks/BreakReveal';
import SharePrompt from '../../components/ui/SharePrompt';

const chapter = chapters[2];
---

<ChapterLayout chapter={chapter} locale="gu">

  <!-- ═══ HERO ═══ -->
  <section class="min-h-[80vh] flex flex-col items-center justify-end px-6 pb-20 relative overflow-hidden">
    <div class="chapter-number" style={`color: ${chapter.accent};`}>3</div>

    <div class="max-w-[680px] mx-auto text-center relative z-10">
      <FadeInBlock client:visible>
        <p class="text-[13px] italic text-subtle/60 mb-5" style="font-family: var(--font-body);">
          અધ્યાય ત્રણ
        </p>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.15}>
        <h1
          class="mb-8 leading-[1.08]"
          style={`font-family: var(--font-heading); color: ${chapter.accent}; font-size: clamp(2.8rem, 7vw, 4.5rem); font-weight: 800; letter-spacing: -0.03em;`}
        >
          Context<br />Engineering
        </h1>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.3}>
        <p class="text-lg md:text-xl italic text-deep/40 leading-relaxed max-w-sm mx-auto" style="font-family: var(--font-body);">
          AI ભૂલતું નથી &mdash; એ પહેલેથી<br />યાદ જ નહોતું રાખતું.
        </p>
      </FadeInBlock>
    </div>

    <div class="absolute bottom-6 left-1/2 -translate-x-1/2 opacity-20 animate-pulse">
      <div class="w-px h-10 bg-deep/40"></div>
    </div>
  </section>

  <!-- ═══ OPENING HOOK ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-24 pb-16">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] drop-cap" style="margin: 0;">
        દર વખતે જ્યારે તું AI ને message મોકલે, ત્યારે એ તારી <em>આખી conversation</em> scratch થી વાંચે &mdash; તેં મોકલેલો દરેક message, AI નો દરેક response &mdash; reply generate કરે, અને પછી બધું ભૂલી જાય. આગલી turn, same thing. આખી transcript ફરીથી feed થાય. દુનિયાનો સૌથી diligent પણ amnesiac reader: દર વખતે completely thorough, દરેક single turn માં zero થી start.
      </p>
    </FadeInBlock>
  </section>

  <Divider accent={chapter.accent} client:load />

  <!-- ═══ CONCEPT SETUP ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-16">
    <FadeInBlock client:visible>
      <AccentBar color={chapter.accent} className="w-16 mb-10" client:visible />
      <h2 class="mb-8" style="font-family: var(--font-heading);">
        Window ને Edges છે
      </h2>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] mb-10" style="margin-top: 0;">
        એ transcript જે AI દર turn માં ફરી વાંચે? એ <strong>context window</strong> નામની વસ્તુમાં રહે &mdash; <strong>tokens</strong> માં measure થાય (roughly એક token દોણા-ત્રણ ચોથા word જેટલું). Claude ની window લગભગ 200,000 tokens hold કરે &mdash; 500-page book. Huge લાગે, પણ તારો દરેક message, દરેક response, અને દરેક hidden system instruction &mdash; બધા same budget માંથી ખાય. ભરાય એટલું faster જેટલું લાગે.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.15}>
      <p class="text-lg leading-[1.85] mb-10">
        Budget ખતમ થાય ત્યારે, સૌથી જૂના messages edge પરથી fall થાય. Message three માં dietary restrictions mention કર્યા? Message fifteen સુધી AI કદાચ એ ન જોઈ શકે. એ careless નથી &mdash; literally જે સામે નથી એ see ન કરી શકે. AI ફક્ત currently window માં રહેલી information જેટલું good છે.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.2}>
      <p class="text-lg leading-[1.85] mb-14">
        આ કારણે best AI users ફક્ત good prompts ન લખે &mdash; એ window માં શું <em>જાય</em> એ manage કરે. Periodically summarize કર જેથી key details visible રહે. Important stuff પહેલાં મૂક. Topic shift થાય ત્યારે fresh start કર. Structured formats વાપર જેથી AI context efficiently parse કરી શકે. <strong>Context engineering એ new prompt engineering છે.</strong>
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.25}>
      <div class="insight-box">
        <p style={`font-family: var(--font-heading); font-size: 0.85rem; font-weight: 700; color: ${chapter.accent}; margin: 0 0 0.5rem;`}>મુખ્ય સમજ</p>
        <p style="font-size: 1rem; line-height: 1.75; margin: 0;">
          Good prompt લખવું step one છે. AI ની સામે કઈ information મૂકવી &mdash; અને કેવી structure કરવી &mdash; ત્યાં real leverage છે. Best prompt પણ fail થાય જો context ખોટું હોય.
        </p>
      </div>
    </FadeInBlock>
  </section>

  <!-- ═══ WIDGET BRIDGE ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-4 pb-8">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] text-center mx-auto italic text-deep/50" style="font-family: var(--font-body); text-wrap: balance;">
        Context window ભરાતી, overflow થતી, અને ભૂલતી &mdash; real time માં જો.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ THE WIDGET ═══ -->
  <section class="max-w-[900px] mx-auto px-6 md:px-10 py-8">
    <ContextWindowViz client:visible />
  </section>

  <Divider accent={chapter.accent} client:load />

  <SharePrompt text="આખરે સમજાયું AI conversation વચ્ચે કેમ 'ભૂલી' જાય છે. Memory નથી — context window છે. આ course live demos સાથે એટલું clearly explain કરે." client:visible />

  <!-- ═══ SEND-OFF ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-24">
    <FadeInBlock client:visible>
      <div class="pull-quote" style={`border-color: ${chapter.accent}; max-width: 50ch; margin: 0 auto 2rem;`}>
        હવે તું whiteboard જોઈ શકે. Window ના edges feel કરે. એ AI સાથેની દરેક conversation બદલી નાખે.
      </div>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] text-center mx-auto" style="max-width: 55ch;">
        અત્યાર સુધી chatbots સાથે કામ કર્યું. હવે zoom out &mdash; chat window ની બહાર સેંકડો AI tools exist કરે, અને landscape જાણવાથી possible શું છે એ view બદલાય.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ BREAK ═══ -->
  <section id="break" class="py-16 px-6" style="background: linear-gradient(180deg, var(--color-cream) 0%, rgba(123,97,255,0.06) 15%, rgba(123,97,255,0.06) 85%, var(--color-cream) 100%);">
    <div class="max-w-[900px] mx-auto">
      <BreakReveal accentColor="#7B61FF" toolName="Vibe Check" client:visible>
        <VibeCheck client:visible />
      </BreakReveal>
    </div>
  </section>

</ChapterLayout>
