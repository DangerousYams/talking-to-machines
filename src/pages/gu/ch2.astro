---
export const prerender = true;
import ChapterLayout from '../../layouts/ChapterLayout.astro';
import { chapters } from '../../data/chapters';
import FadeInBlock from '../../components/scroll/FadeInBlock';
import AccentBar from '../../components/ui/AccentBar';
import Divider from '../../components/ui/Divider';
import FlipTheScript from '../../components/widgets/ch2/FlipTheScript';
import SocraticSmackdown from '../../components/breaks/SocraticSmackdown';
import BreakReveal from '../../components/breaks/BreakReveal';
import SharePrompt from '../../components/ui/SharePrompt';
import ForYouCard from '../../components/ui/ForYouCard';

const chapter = chapters[1];
const conceptsSummary = chapter.concepts.map(c => c.name).join(', ');
---

<ChapterLayout chapter={chapter} locale="gu">

  <!-- ═══ HERO ═══ -->
  <section class="min-h-[80vh] flex flex-col items-center justify-end px-6 pb-20 relative overflow-hidden">
    <div class="chapter-number" style={`color: ${chapter.accent};`}>2</div>

    <div class="max-w-[680px] mx-auto text-center relative z-10">
      <FadeInBlock client:visible>
        <p class="text-[13px] italic text-subtle/60 mb-5" style="font-family: var(--font-body);">
          પ્રકરણ બે
        </p>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.15}>
        <h1
          class="mb-8 leading-[1.08]"
          style={`font-family: var(--font-heading); color: ${chapter.accent}; font-size: clamp(2.8rem, 7vw, 4.5rem); font-weight: 800; letter-spacing: -0.03em;`}
        >
          પૂછવાની<br />કળા
        </h1>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.3}>
        <p class="text-lg md:text-xl italic text-deep/40 leading-relaxed max-w-sm mx-auto" style="font-family: var(--font-body);">
          સૌથી સારો prompt એ છે જે<br />AI ને તને સવાલ પૂછવા મજબૂર કરે.
        </p>
      </FadeInBlock>
    </div>

    <div class="absolute bottom-6 left-1/2 -translate-x-1/2 opacity-20 animate-pulse">
      <div class="w-px h-10 bg-deep/40"></div>
    </div>
  </section>

  <!-- ═══ OPENING HOOK ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-24 pb-16">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] drop-cap" style="margin: 0;">
        મોટાભાગના લોકો AI ને vending machine ની જેમ વાપરે છે. Prompt નાખ, જવાબ મળ. પણ સૌથી સારા results <em>conversations</em> માંથી આવે, commands માંથી નહીં. જે માણસ ફક્ત prompt ટાઈપ કરે અને જે માણસ prompt craft કરે — એ બંનેમાં એ જ ફરક છે જે 2,400 વર્ષથી મહાન વિચારકોને power આપતો આવ્યો છે: સાચા સવાલ પૂછવા.
      </p>
    </FadeInBlock>
  </section>

  <Divider accent={chapter.accent} client:load />

  <!-- ═══ THE SOCRATIC FLIP ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-16">
    <FadeInBlock client:visible>
      <AccentBar color={chapter.accent} className="w-16 mb-10" client:visible />
      <h2 class="mb-8" style="font-family: var(--font-heading);">
        The Socratic Flip
      </h2>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] mb-14" style="margin-top: 0;">
        Socrates ક્યારેય સવાલનો સીધો જવાબ નહોતા આપતા. એ <em>વધુ</em> સવાલ પૂછતા, જ્યાં સુધી એમના students ઊંડી સમજ સુધી ન પહોંચે. તું AI સાથે આ જ trick વાપરી શકે, પણ ઉલટી: જવાબ માંગવાને બદલે, AI ને કહે કે <em>તને</em> interview કરે. જ્યારે તું ટાઈપ કરે "birthday party plan કર," તું ઘણી બધી details છોડી દે છે જે answer ને ધરમૂળથી બદલી નાખે. AI ને ખબર નથી — પણ એ પૂછી શકે.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.2}>
      <p class="text-lg leading-[1.85] mb-14">
        આ એક technique — કોણ સવાલ પૂછે એ flip કરવું — AI ને answer machine માંથી <strong>thinking partner</strong> માં બદલી નાખે. એને permission આપ probe કરવાની, clarify કરવાની, challenge કરવાની. પાંચ back-and-forth સવાલ પછીનો output, single-shot command કરતાં ઓળખાય જ નહીં.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.3}>
      <div class="insight-box">
        <p style={`font-family: var(--font-heading); font-size: 0.85rem; font-weight: 700; color: ${chapter.accent}; margin: 0 0 0.5rem;`}>મુખ્ય વાત</p>
        <p style="font-size: 1rem; line-height: 1.75; margin: 0;">
          સૌથી સારા AI users topic ના experts નથી — એ પૂછવામાં expert છે. અને સૌથી powerful સવાલ જે તું પૂછી શકે: "જવાબ આપતા પહેલાં, તારે શું જાણવું જોઈએ?"
        </p>
      </div>
    </FadeInBlock>
  </section>

  <!-- ═══ WIDGET BRIDGE ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-4 pb-8">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] text-center mx-auto italic text-deep/50" style="font-family: var(--font-body); text-wrap: balance;">
        જાતે try કર. એક goal જણાવ — અને AI ને script flip કરતું જો.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ THE WIDGET ═══ -->
  <section id="flip-the-script" class="max-w-[900px] mx-auto px-6 md:px-10 py-8">
    <FlipTheScript client:visible />
  </section>

  <Divider accent={chapter.accent} client:load />

  <div class="py-8">
    <ForYouCard
      chapterSlug={chapter.slug}
      chapterTitle={chapter.title}
      chapterConcepts={conceptsSummary}
      accentColor={chapter.accent}
      client:load
    />
  </div>
  <SharePrompt text="આ AI course એ prompting વિશે મારી વિચારસરણી જ બદલી નાખી. AI ને શું કરવું એ કહેવાને બદલે, તું એને કહે કે પહેલાં તારો INTERVIEW લે. Result માં જમીન-આસમાનનો ફરક." client:visible />

  <!-- ═══ SEND-OFF ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-24">
    <FadeInBlock client:visible>
      <div class="pull-quote" style={`border-color: ${chapter.accent}; max-width: 50ch; margin: 0 auto 2rem;`}>
        AI ને શું વિચારવું એ કહેવાનું બંધ કર. એને તારી સાથે વિચારવાની જગ્યા આપ.
      </div>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] text-center mx-auto" style="max-width: 55ch;">
        પણ તું જે પણ technique શીખ્યો, એ બધી એક જ અદૃશ્ય દીવાલ સામે અથડાય છે: <em>context window</em>. આગળ, આપણે એને ખોલીશું અને શીખીશું કે AI ખરેખર શું જુએ છે.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ BREAK ═══ -->
  <section id="break" class="py-16 px-6" style="background: linear-gradient(180deg, var(--color-cream) 0%, rgba(22,199,154,0.06) 15%, rgba(22,199,154,0.06) 85%, var(--color-cream) 100%);">
    <div class="max-w-[900px] mx-auto">
      <BreakReveal accentColor="#16C79A" toolName="Socratic Smackdown" client:visible>
        <SocraticSmackdown client:visible />
      </BreakReveal>
    </div>
  </section>

</ChapterLayout>
