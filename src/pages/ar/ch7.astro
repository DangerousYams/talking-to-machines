---
export const prerender = true;
import ChapterLayout from '../../layouts/ChapterLayout.astro';
import { chapters } from '../../data/chapters';
import FadeInBlock from '../../components/scroll/FadeInBlock';
import AccentBar from '../../components/ui/AccentBar';
import Divider from '../../components/ui/Divider';
import WhatWouldYouBuild from '../../components/widgets/ch7/WhatWouldYouBuild';
import ShipIt from '../../components/breaks/ShipIt';
import BreakReveal from '../../components/breaks/BreakReveal';
import SharePrompt from '../../components/ui/SharePrompt';

const chapter = chapters[6];
---

<ChapterLayout chapter={chapter} locale="ar">

  <!-- ═══ HERO ═══ -->
  <section class="min-h-[80vh] flex flex-col items-center justify-end px-6 pb-20 relative overflow-hidden">
    <div class="chapter-number" style={`color: ${chapter.accent};`}>7</div>

    <div class="max-w-[680px] mx-auto text-center relative z-10">
      <FadeInBlock client:visible>
        <p class="text-[13px] italic text-subtle/60 mb-5" style="font-family: var(--font-body);">
          الفصل السابع
        </p>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.15}>
        <h1
          class="mb-8 leading-[1.08]"
          style={`font-family: var(--font-heading); color: ${chapter.accent}; font-size: clamp(2.8rem, 7vw, 4.5rem); font-weight: 800; letter-spacing: -0.03em;`}
        >
          إتقان<br />Claude Code
        </h1>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.3}>
        <p class="text-lg md:text-xl italic text-deep/40 leading-relaxed max-w-sm mx-auto" style="font-family: var(--font-body);">
          شريكك البرمجي بالذكاء الاصطناعي، من أول<br />أمر إلى المهارات المخصصة.
        </p>
      </FadeInBlock>
    </div>

    <div class="absolute bottom-6 left-1/2 -translate-x-1/2 opacity-20 animate-pulse">
      <div class="w-px h-10 bg-deep/40"></div>
    </div>
  </section>

  <!-- ═══ OPENING HOOK ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-24 pb-16">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] drop-cap" style="margin: 0;">
        هناك لحظة يتذكرها كل مبرمج. تفتح الطرفية، تكتب طلبًا بلغة طبيعية، وتشاهد الذكاء الاصطناعي يقرأ ملفاتك، ويفهم بنية مشروعك، ويكتب كودًا عبر ملفات متعددة، ويشغله، ويصطدم بخطأ، ويصلح الخطأ، ويشغله مرة أخرى — كل ذلك بدون أن تلمس سطرًا واحدًا. Claude Code ليس إكمالاً تلقائيًا. إنه أداة برمجة <em>وكيلية</em> ترى مشروعك بالكامل، وتجري تعديلات عبر الملفات، وتنفذ أوامر الشِل، وتكرر على أخطائها.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] mt-8">
        لكن الأداة لا تكون أفضل من الإنسان الذي يوجهها. Claude Code يستطيع كتابة ألف سطر من الكود المثالي في ثوانٍ — أو ألف سطر من الهراء الذي يبدو مقنعًا. الفرق هو أنت.
      </p>
    </FadeInBlock>
  </section>

  <Divider accent={chapter.accent} client:load />

  <!-- ═══ THE LOOP ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-16">
    <FadeInBlock client:visible>
      <AccentBar color={chapter.accent} className="w-16 mb-10" client:visible />
      <h2 class="mb-8" style="font-family: var(--font-heading);">
        حدد، ولّد، تحقق
      </h2>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] mb-14" style="margin-top: 0;">
        عندما تعطي Claude Code مهمة، يشغّل حلقة الوكيل: اقرأ قاعدة الكود، خطط التعديلات، اكتب التغييرات، شغّل الكود، أصلح ما انكسر. قد يمر بهذه الحلقة ثلاث، خمس، عشر مرات — كلها في ثوانٍ. وظيفتك لم تعد كتابة كل سطر. بل أن <strong>تحدد</strong> ما تبنيه، وتدع الذكاء الاصطناعي <strong>يولّده</strong>، ثم <strong>تتحقق</strong> من صحته فعلاً. معظم المبتدئين يتخطون الخطوة الأخيرة. يرون كودًا يظهر، يبدو معقولاً، ويمضون قدمًا. لا تكن ذلك الشخص. الكود المولّد هو مسودة أولى. تحققك هو التحرير الذي يجعلها قابلة للنشر.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.2}>
      <p class="text-lg leading-[1.85] mb-14">
        السلاح السري هو ملف <code>CLAUDE.md</code> — ضعه في جذر مشروعك و Claude Code يقرأه في بداية كل جلسة. تقنياتك، واتفاقيات التسمية، وقواعد الأسلوب، ومتطلبات الاختبار — كلها مُشفّرة مرة واحدة حتى لا تكررها أبدًا. إنها هندسة السياق مطبقة على البرمجة.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.3}>
      <div class="insight-box">
        <p style={`font-family: var(--font-heading); font-size: 0.85rem; font-weight: 700; color: ${chapter.accent}; margin: 0 0 0.5rem;`}>الهدف ليس أن تكتب كل سطر — بل أن تفهم كل سطر.</p>
        <p style="font-size: 1rem; line-height: 1.75; margin: 0;">
          إذا لم تستطع قراءة الكود الذي يولده Claude، لا تستطيع التحقق منه. أفضل مستخدمي Claude Code ليسوا من يولّدون أكبر كمية من الكود — إنهم من يطرحون أفضل الأسئلة ويلتقطون أدق الأخطاء.
        </p>
      </div>
    </FadeInBlock>
  </section>

  <!-- ═══ WIDGET BRIDGE ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-4 pb-8">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] text-center mx-auto italic text-deep/50" style="font-family: var(--font-body); text-wrap: balance;">
        شاهد الحلقة وهي تعمل. اكتب طلبًا وراقب Claude Code يعمل.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ THE WIDGET ═══ -->
  <section id="terminal-playground" class="max-w-[900px] mx-auto px-6 md:px-10 py-8">
    <WhatWouldYouBuild client:visible />
  </section>

  <Divider accent={chapter.accent} client:load />

  <SharePrompt text="This chapter on Claude Code is wild — watched an AI pair programmer think through a problem, write code, and debug itself. The future of coding is here." client:visible />

  <!-- ═══ SEND-OFF ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-24">
    <FadeInBlock client:visible>
      <div class="pull-quote" style={`border-color: ${chapter.accent}; max-width: 50ch; margin: 0 auto 2rem;`}>
        Claude Code لا يكتب كودًا من أجلك — بل يكتبه معك.
      </div>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] text-center mx-auto" style="max-width: 55ch;">
        محادثة واحدة تتعامل مع مهمة واحدة. لكن المشاريع الحقيقية فيها عشرات. تاليًا: كيف تقسم الطموحات الكبيرة إلى قطع يستطيع الذكاء الاصطناعي تنفيذها فعلاً.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ BREAK ═══ -->
  <section id="break" class="py-16 px-6" style="background: linear-gradient(180deg, var(--color-cream) 0%, rgba(123,97,255,0.06) 15%, rgba(123,97,255,0.06) 85%, var(--color-cream) 100%);">
    <div class="max-w-[900px] mx-auto">
      <BreakReveal accentColor="#7B61FF" toolName="Ship It" client:visible>
        <ShipIt client:visible />
      </BreakReveal>
    </div>
  </section>

</ChapterLayout>
