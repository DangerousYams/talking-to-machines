---
export const prerender = true;
import ChapterLayout from '../../layouts/ChapterLayout.astro';
import { chapters } from '../../data/chapters';
import FadeInBlock from '../../components/scroll/FadeInBlock';
import AccentBar from '../../components/ui/AccentBar';
import Divider from '../../components/ui/Divider';
import JobTransformer from '../../components/widgets/ch10/JobTransformer';
import IrreplaceableYou from '../../components/breaks/IrreplaceableYou';
import BreakReveal from '../../components/breaks/BreakReveal';
import SharePrompt from '../../components/ui/SharePrompt';

const chapter = chapters[9];
---

<ChapterLayout chapter={chapter} locale="ar">

  <!-- ═══ HERO ═══ -->
  <section class="min-h-[80vh] flex flex-col items-center justify-end px-6 pb-20 relative overflow-hidden">
    <div class="chapter-number" style={`color: ${chapter.accent};`}>10</div>

    <div class="max-w-[680px] mx-auto text-center relative z-10">
      <FadeInBlock client:visible>
        <p class="text-[13px] italic text-subtle/60 mb-5" style="font-family: var(--font-body);">
          الفصل العاشر
        </p>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.15}>
        <h1
          class="mb-8 leading-[1.08]"
          style={`font-family: var(--font-heading); color: ${chapter.accent}; font-size: clamp(2.8rem, 7vw, 4.5rem); font-weight: 800; letter-spacing: -0.03em;`}
        >
          الميزة<br />البشرية
        </h1>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.3}>
        <p class="text-lg md:text-xl italic text-deep/40 leading-relaxed max-w-sm mx-auto" style="font-family: var(--font-body);">
          ما لا يستطيع الذكاء الاصطناعي استبداله &mdash;<br />وما يجعله أسهل 10 مرات.
        </p>
      </FadeInBlock>
    </div>

    <div class="absolute bottom-6 left-1/2 -translate-x-1/2 opacity-20 animate-pulse">
      <div class="w-px h-10 bg-deep/40"></div>
    </div>
  </section>

  <!-- ═══ OPENING HOOK ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-24 pb-16">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] drop-cap" style="margin: 0;">
        كل مرة تظهر قدرة جديدة للذكاء الاصطناعي، يظهر نفس العنوان: <em>"هل سيستبدل الذكاء الاصطناعي [أدخل الوظيفة]؟"</em> إنه السؤال الخاطئ. الذكاء الاصطناعي لا يستبدل البشر. يستبدل <strong>المهام</strong>. وعندما تدقق في أي وظيفة — أي مهنة، أي سعي إبداعي — تجد مزيجًا: بعض المهام يتعامل معها الذكاء الاصطناعي ببراعة، وبعضها يساعد فيها، وبعضها يبقى بعناد وجمال بشريًا.
      </p>
    </FadeInBlock>
  </section>

  <Divider accent={chapter.accent} client:load />

  <!-- ═══ JOBS SHAPE-SHIFT ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-16">
    <FadeInBlock client:visible>
      <AccentBar color={chapter.accent} className="w-16 mb-10" client:visible />
      <h2 class="mb-8" style="font-family: var(--font-heading);">
        الوظائف لا تختفي — بل تتحول
      </h2>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] mb-10" style="margin-top: 0;">
        عندما ظهرت أجهزة الصراف الآلي، توقع الجميع أن صرافي البنوك سيختفون. بدلاً من ذلك، أعدادهم <em>زادت</em> — لأن أجهزة الصراف جعلت فتح فروع جديدة أرخص، ووظيفة الصراف تحولت من عد النقود إلى بناء العلاقات. الذكاء الاصطناعي يفعل الشيء نفسه، لكن أسرع وعبر كل صناعة في وقت واحد. المسمى الوظيفي يبقى نفسه. <em>الوصف الوظيفي</em> يتغير بشكل جذري.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.2}>
      <p class="text-lg leading-[1.85] mb-10">
        إدخال البيانات؟ الذكاء الاصطناعي يتعامل معها. المسودات الأولى؟ جيدة بشكل مذهل. لكن <em>اختيار ما تبنيه</em>، و<em>قراءة الموقف في أزمة</em>، و<em>تحديد ما تعنيه البيانات</em> — هذه تتطلب حكمًا وتعاطفًا ورؤية يفتقر إليها الذكاء الاصطناعي جذريًا.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.3}>
      <div class="insight-box">
        <p style={`font-family: var(--font-heading); font-size: 0.85rem; font-weight: 700; color: ${chapter.accent}; margin: 0 0 0.5rem;`}>فكرة أساسية</p>
        <p style="font-size: 1rem; line-height: 1.75; margin: 0;">
          ليست "البشر ضد الذكاء الاصطناعي." بل <strong>البشر مع الذكاء الاصطناعي ضد البشر بدون الذكاء الاصطناعي</strong>. الناس الذين يتعلمون العمل <em>مع</em> أدوات الذكاء الاصطناعي لا يفقدون وظائفهم — يؤدون وظائفهم أسرع وأفضل ومع قدرات لم تكن لديهم من قبل.
        </p>
      </div>
    </FadeInBlock>
  </section>

  <!-- ═══ WIDGET BRIDGE ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-4 pb-8">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] text-center mx-auto italic text-deep/50" style="max-width: 45ch; font-family: var(--font-body);">
        اختر مهنة أدناه وشاهد بالضبط كيف يحولها الذكاء الاصطناعي من الداخل. لا تختفي مهمة واحدة بالكامل.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ THE WIDGET ═══ -->
  <section class="max-w-[900px] mx-auto px-6 md:px-10 py-8">
    <JobTransformer client:visible />
  </section>

  <Divider accent={chapter.accent} client:load />

  <SharePrompt text="This course doesn't hype AI or fear-monger about jobs. It maps exactly which skills stay human and which get augmented. Refreshingly honest." client:visible />

  <!-- ═══ SEND-OFF ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-24">
    <FadeInBlock client:visible>
      <div class="pull-quote" style={`border-color: ${chapter.accent}; max-width: 50ch; margin: 0 auto 2rem;`}>
        الذكاء الاصطناعي يولّد الخيارات. أنت تختار الصحيح. ذلك الحكم — الذوق، التعاطف، الرؤية، التفكير من المبادئ الأولى — هو ميزتك.
      </div>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] text-center mx-auto" style="max-width: 55ch;">
        بقي فصل واحد. لا مزيد من النظريات. حان وقت دمج كل ما تعلمته وبناء شيء حقيقي &mdash; شيء لن يفكر في صنعه إلا أنت.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ BREAK ═══ -->
  <section id="break" class="py-16 px-6" style="background: linear-gradient(180deg, var(--color-cream) 0%, rgba(22,199,154,0.06) 15%, rgba(22,199,154,0.06) 85%, var(--color-cream) 100%);">
    <div class="max-w-[900px] mx-auto">
      <BreakReveal accentColor="#16C79A" toolName="Irreplaceable You" client:visible>
        <IrreplaceableYou client:visible />
      </BreakReveal>
    </div>
  </section>

</ChapterLayout>
