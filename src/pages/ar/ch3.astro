---
export const prerender = true;
import ChapterLayout from '../../layouts/ChapterLayout.astro';
import { chapters } from '../../data/chapters';
import FadeInBlock from '../../components/scroll/FadeInBlock';
import AccentBar from '../../components/ui/AccentBar';
import Divider from '../../components/ui/Divider';
import ContextWindowViz from '../../components/widgets/ch3/ContextWindowViz';
import VibeCheck from '../../components/breaks/VibeCheck';
import BreakReveal from '../../components/breaks/BreakReveal';
import SharePrompt from '../../components/ui/SharePrompt';

const chapter = chapters[2];
---

<ChapterLayout chapter={chapter} locale="ar">

  <!-- ═══ HERO ═══ -->
  <section class="min-h-[80vh] flex flex-col items-center justify-end px-6 pb-20 relative overflow-hidden">
    <div class="chapter-number" style={`color: ${chapter.accent};`}>3</div>

    <div class="max-w-[680px] mx-auto text-center relative z-10">
      <FadeInBlock client:visible>
        <p class="text-[13px] italic text-subtle/60 mb-5" style="font-family: var(--font-body);">
          الفصل الثالث
        </p>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.15}>
        <h1
          class="mb-8 leading-[1.08]"
          style={`font-family: var(--font-heading); color: ${chapter.accent}; font-size: clamp(2.8rem, 7vw, 4.5rem); font-weight: 800; letter-spacing: -0.03em;`}
        >
          هندسة<br />السياق
        </h1>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.3}>
        <p class="text-lg md:text-xl italic text-deep/40 leading-relaxed max-w-sm mx-auto" style="font-family: var(--font-body);">
          الذكاء الاصطناعي لا ينسى &mdash; هو لم يكن<br />يتذكر من الأساس.
        </p>
      </FadeInBlock>
    </div>

    <div class="absolute bottom-6 left-1/2 -translate-x-1/2 opacity-20 animate-pulse">
      <div class="w-px h-10 bg-deep/40"></div>
    </div>
  </section>

  <!-- ═══ OPENING HOOK ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-24 pb-16">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] drop-cap" style="margin: 0;">
        في كل مرة ترسل رسالة للذكاء الاصطناعي، يقرأ <em>محادثتكما كاملة</em> من الصفر &mdash; كل رسالة أرسلتها، كل إجابة أعطاها &mdash; يولّد ردًا، ثم ينسى كل شيء. في المرة التالية، نفس الشيء. النص الكامل يُعاد تغذيته مجددًا. إنه القارئ الأكثر اجتهادًا لكن المصاب بفقدان الذاكرة: شامل تمامًا في كل مرة، يبدأ من الصفر في كل دورة.
      </p>
    </FadeInBlock>
  </section>

  <Divider accent={chapter.accent} client:load />

  <!-- ═══ CONCEPT SETUP ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-16">
    <FadeInBlock client:visible>
      <AccentBar color={chapter.accent} className="w-16 mb-10" client:visible />
      <h2 class="mb-8" style="font-family: var(--font-heading);">
        للنافذة حدود
      </h2>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] mb-10" style="margin-top: 0;">
        ذلك النص الذي يعيد الذكاء الاصطناعي قراءته في كل دورة؟ يعيش داخل شيء يسمى <strong>نافذة السياق</strong> &mdash; تُقاس بـ<strong>الرموز</strong> (تقريبًا رمز واحد لكل ثلاثة أرباع كلمة). نافذة سياق Claude تتسع لنحو 200,000 رمز &mdash; كتاب من 500 صفحة. يبدو ضخمًا، لكن كل رسالة ترسلها، كل إجابة تستقبلها، وكل تعليمة نظام مخفية تستهلك من نفس الميزانية. تمتلئ أسرع مما تعتقد.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.15}>
      <p class="text-lg leading-[1.85] mb-10">
        عندما تنفد الميزانية، أقدم الرسائل تسقط من الحافة. ذكرت قيودك الغذائية في الرسالة الثالثة؟ بحلول الرسالة الخامسة عشرة قد لا يراها الذكاء الاصطناعي بعد الآن. ليس مهملاً &mdash; إنه حرفيًا لا يستطيع رؤية ما لم يعد أمامه. الذكاء الاصطناعي جيد فقط بقدر المعلومات الموجودة حاليًا في نافذته.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.2}>
      <p class="text-lg leading-[1.85] mb-14">
        لهذا السبب أفضل مستخدمي الذكاء الاصطناعي لا يكتبون توجيهات جيدة فحسب &mdash; بل يديرون ما يدخل <em>في</em> النافذة. لخّص بشكل دوري حتى تبقى التفاصيل المهمة مرئية. ضع الأهم في المقدمة. ابدأ من جديد عندما يتغير الموضوع. استخدم تنسيقات منظمة ليتمكن الذكاء الاصطناعي من تحليل سياقك بكفاءة. <strong>هندسة السياق هي هندسة التوجيهات الجديدة.</strong>
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.25}>
      <div class="insight-box">
        <p style={`font-family: var(--font-heading); font-size: 0.85rem; font-weight: 700; color: ${chapter.accent}; margin: 0 0 0.5rem;`}>فكرة أساسية</p>
        <p style="font-size: 1rem; line-height: 1.75; margin: 0;">
          كتابة توجيه جيد هي الخطوة الأولى. تحديد المعلومات التي تضعها أمام الذكاء الاصطناعي &mdash; وكيفية تنظيمها &mdash; هو المكان الذي توجد فيه الرافعة الحقيقية. أفضل توجيه في العالم يفشل إذا كان السياق خاطئًا.
        </p>
      </div>
    </FadeInBlock>
  </section>

  <!-- ═══ WIDGET BRIDGE ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-4 pb-8">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] text-center mx-auto italic text-deep/50" style="max-width: 45ch; font-family: var(--font-body);">
        شاهد نافذة السياق تمتلئ، وتفيض، وتنسى &mdash; في الوقت الحقيقي.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ THE WIDGET ═══ -->
  <section class="max-w-[900px] mx-auto px-6 md:px-10 py-8">
    <ContextWindowViz client:visible />
  </section>

  <Divider accent={chapter.accent} client:load />

  <SharePrompt text="Finally understand why AI 'forgets' things mid-conversation. It's not memory — it's a context window. This course explains it so clearly with live demos." client:visible />

  <!-- ═══ SEND-OFF ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-24">
    <FadeInBlock client:visible>
      <div class="pull-quote" style={`border-color: ${chapter.accent}; max-width: 50ch; margin: 0 auto 2rem;`}>
        أنت ترى السبورة الآن. تشعر بحدود النافذة. هذا يغير كل محادثة تخوضها مع الذكاء الاصطناعي.
      </div>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] text-center mx-auto" style="max-width: 55ch;">
        حتى الآن كنا نعمل مع روبوتات المحادثة. تاليًا، نبتعد قليلاً &mdash; مئات أدوات الذكاء الاصطناعي موجودة خارج نافذة المحادثة، ومعرفة المشهد تغيّر ما تعتقد أنه ممكن.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ BREAK ═══ -->
  <section id="break" class="py-16 px-6" style="background: linear-gradient(180deg, var(--color-cream) 0%, rgba(123,97,255,0.06) 15%, rgba(123,97,255,0.06) 85%, var(--color-cream) 100%);">
    <div class="max-w-[900px] mx-auto">
      <BreakReveal accentColor="#7B61FF" toolName="Vibe Check" client:visible>
        <VibeCheck client:visible />
      </BreakReveal>
    </div>
  </section>

</ChapterLayout>
