---
export const prerender = true;
import ChapterLayout from '../../layouts/ChapterLayout.astro';
import { chapters } from '../../data/chapters';
import FadeInBlock from '../../components/scroll/FadeInBlock';
import AccentBar from '../../components/ui/AccentBar';
import Divider from '../../components/ui/Divider';
import FlipTheScript from '../../components/widgets/ch2/FlipTheScript';
import SocraticSmackdown from '../../components/breaks/SocraticSmackdown';
import BreakReveal from '../../components/breaks/BreakReveal';
import SharePrompt from '../../components/ui/SharePrompt';

const chapter = chapters[1];
---

<ChapterLayout chapter={chapter} locale="es">

  <!-- ═══ HERO ═══ -->
  <section class="min-h-[80vh] flex flex-col items-center justify-end px-6 pb-20 relative overflow-hidden">
    <div class="chapter-number" style={`color: ${chapter.accent};`}>2</div>

    <div class="max-w-[680px] mx-auto text-center relative z-10">
      <FadeInBlock client:visible>
        <p class="text-[13px] italic text-subtle/60 mb-5" style="font-family: var(--font-body);">
          Capítulo dos
        </p>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.15}>
        <h1
          class="mb-8 leading-[1.08]"
          style={`font-family: var(--font-heading); color: ${chapter.accent}; font-size: clamp(2.8rem, 7vw, 4.5rem); font-weight: 800; letter-spacing: -0.03em;`}
        >
          El arte de<br />preguntar
        </h1>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.3}>
        <p class="text-lg md:text-xl italic text-deep/40 leading-relaxed max-w-sm mx-auto" style="font-family: var(--font-body);">
          El mejor prompt es una pregunta que<br />hace que la IA te pregunte a ti.
        </p>
      </FadeInBlock>
    </div>

    <div class="absolute bottom-6 left-1/2 -translate-x-1/2 opacity-20 animate-pulse">
      <div class="w-px h-10 bg-deep/40"></div>
    </div>
  </section>

  <!-- ═══ OPENING HOOK ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-24 pb-16">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] drop-cap" style="margin: 0;">
        La mayoría de la gente usa la IA como una máquina expendedora. Inserta prompt, recibe respuesta. Pero los mejores resultados vienen de <em>conversaciones</em>, no de comandos. La diferencia entre alguien que escribe un prompt y alguien que lo crea con cuidado es la misma diferencia que ha impulsado a los grandes pensadores durante 2,400 años: hacer las preguntas correctas.
      </p>
    </FadeInBlock>
  </section>

  <Divider accent={chapter.accent} client:load />

  <!-- ═══ THE SOCRATIC FLIP ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-16">
    <FadeInBlock client:visible>
      <AccentBar color={chapter.accent} className="w-16 mb-10" client:visible />
      <h2 class="mb-8" style="font-family: var(--font-heading);">
        El giro socrático
      </h2>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] mb-14" style="margin-top: 0;">
        Sócrates nunca respondía una pregunta directamente. Hacía <em>más</em> preguntas hasta que sus estudiantes llegaban a un entendimiento más profundo. Puedes usar el mismo truco con la IA, pero al revés: en lugar de pedir una respuesta, pídele a la IA que te entreviste <em>a ti</em>. Cuando escribes "planifica una fiesta de cumpleaños", estás omitiendo docenas de detalles que cambian masivamente la respuesta. La IA no los sabe — pero puede preguntar.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.2}>
      <p class="text-lg leading-[1.85] mb-14">
        Esta sola técnica — cambiar quién hace las preguntas — convierte a la IA de una máquina de respuestas en un <strong>compañero de pensamiento</strong>. Dale permiso de indagar, aclarar y cuestionar. El resultado después de cinco preguntas de ida y vuelta será irreconocible comparado con un comando de un solo intento.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.3}>
      <div class="insight-box">
        <p style={`font-family: var(--font-heading); font-size: 0.85rem; font-weight: 700; color: ${chapter.accent}; margin: 0 0 0.5rem;`}>Punto clave</p>
        <p style="font-size: 1rem; line-height: 1.75; margin: 0;">
          Los mejores usuarios de IA no son expertos en el tema — son expertos en preguntar. Y la pregunta más poderosa que puedes hacer es: "Antes de responder, ¿qué necesitas saber?"
        </p>
      </div>
    </FadeInBlock>
  </section>

  <!-- ═══ WIDGET BRIDGE ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-4 pb-8">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] text-center mx-auto italic text-deep/50" style="max-width: 45ch; font-family: var(--font-body);">
        Pruébalo tú mismo. Plantea un objetivo — y mira cómo la IA le da la vuelta.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ THE WIDGET ═══ -->
  <section id="flip-the-script" class="max-w-[900px] mx-auto px-6 md:px-10 py-8">
    <FlipTheScript client:visible />
  </section>

  <Divider accent={chapter.accent} client:load />

  <SharePrompt text="Este curso de IA cambió por completo cómo pienso sobre los prompts. En lugar de decirle a la IA qué hacer, le pides que te entreviste a TI primero. Los resultados son de otro nivel." client:visible />

  <!-- ═══ SEND-OFF ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-24">
    <FadeInBlock client:visible>
      <div class="pull-quote" style={`border-color: ${chapter.accent}; max-width: 50ch; margin: 0 auto 2rem;`}>
        Deja de decirle a la IA qué pensar. Empieza a darle espacio para pensar contigo.
      </div>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] text-center mx-auto" style="max-width: 55ch;">
        Pero cada técnica que has aprendido se topa con la misma pared invisible: la <em>ventana de contexto</em>. A continuación, la abrimos de par en par y aprendemos cómo diseñar lo que la IA realmente ve.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ BREAK ═══ -->
  <section id="break" class="py-16 px-6" style="background: linear-gradient(180deg, var(--color-cream) 0%, rgba(22,199,154,0.06) 15%, rgba(22,199,154,0.06) 85%, var(--color-cream) 100%);">
    <div class="max-w-[900px] mx-auto">
      <BreakReveal accentColor="#16C79A" toolName="Socratic Smackdown" client:visible>
        <SocraticSmackdown client:visible />
      </BreakReveal>
    </div>
  </section>

</ChapterLayout>
