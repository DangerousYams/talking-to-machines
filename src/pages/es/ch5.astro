---
export const prerender = true;
import ChapterLayout from '../../layouts/ChapterLayout.astro';
import { chapters } from '../../data/chapters';
import FadeInBlock from '../../components/scroll/FadeInBlock';
import AccentBar from '../../components/ui/AccentBar';
import Divider from '../../components/ui/Divider';
import TrustThermometer from '../../components/widgets/ch5/TrustThermometer';
import WouldYouLetIt from '../../components/breaks/WouldYouLetIt';
import BreakReveal from '../../components/breaks/BreakReveal';
import SharePrompt from '../../components/ui/SharePrompt';

const chapter = chapters[4];
---

<ChapterLayout chapter={chapter} locale="es">

  <!-- ═══ HERO ═══ -->
  <section class="min-h-[80vh] flex flex-col items-center justify-end px-6 pb-20 relative overflow-hidden">
    <div class="chapter-number" style={`color: ${chapter.accent};`}>5</div>

    <div class="max-w-[680px] mx-auto text-center relative z-10">
      <FadeInBlock client:visible>
        <p class="text-[13px] italic text-subtle/60 mb-5" style="font-family: var(--font-body);">
          Capítulo cinco
        </p>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.15}>
        <h1
          class="mb-8 leading-[1.08]"
          style={`font-family: var(--font-heading); color: ${chapter.accent}; font-size: clamp(2.8rem, 7vw, 4.5rem); font-weight: 800; letter-spacing: -0.03em;`}
        >
          Dale<br />herramientas
        </h1>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.3}>
        <p class="text-lg md:text-xl italic text-deep/40 leading-relaxed max-w-sm mx-auto" style="font-family: var(--font-body);">
          El salto de "responder preguntas"<br />a "tomar acciones".
        </p>
      </FadeInBlock>
    </div>

    <div class="absolute bottom-6 left-1/2 -translate-x-1/2 opacity-20 animate-pulse">
      <div class="w-px h-10 bg-deep/40"></div>
    </div>
  </section>

  <!-- ═══ OPENING HOOK ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-24 pb-16">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] drop-cap" style="margin: 0; --drop-cap-color: #F5A623;">
        Hasta ahora, cada interacción que has tenido con la IA sigue el mismo patrón. Escribes algo. La IA te responde algo. Un autocompletado realmente sofisticado. ¿Pero qué pasaría si la IA pudiera <em>hacer cosas</em>? No solo contarte sobre el clima, sino revisar el pronóstico. No solo escribir código, sino ejecutarlo. No solo sugerirte qué buscar en Google, sino buscar en la web ella misma y traerte lo que encontró.
      </p>
    </FadeInBlock>
  </section>

  <Divider accent={chapter.accent} client:load />

  <!-- ═══ THE TRUST QUESTION ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-16">
    <FadeInBlock client:visible>
      <AccentBar color={chapter.accent} className="w-16 mb-10" client:visible />
      <h2 class="mb-8" style="font-family: var(--font-heading);">
        La cuestión de la confianza
      </h2>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] mb-14" style="margin-top: 0;">
        Cuando le das herramientas a la IA, le entregas una caja de herramientas y le dices: <em>"Aquí hay cosas que puedes hacer. Tú decides cuándo usarlas."</em> Puede buscar en la web, ejecutar código, leer archivos, llamar APIs. La IA no se volvió más inteligente — se <strong>conectó</strong>. Y eso plantea una pregunta que no tiene nada que ver con tecnología y todo que ver contigo: ¿cuánto debería hacer la IA por su cuenta antes de consultarte?
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.15}>
      <p class="text-lg leading-[1.85] mb-14">
        La respuesta depende de tres cosas. <strong>Riesgo</strong> — ¿qué es lo peor que podría pasar? <strong>Reversibilidad</strong> — ¿puedes deshacerlo? Y <strong>confianza</strong> — ¿qué tan bien conoces esta herramienta? Una calculadora que has usado cien veces merece más libertad que un plugin nuevo que nunca has probado.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.2}>
      <div class="insight-box">
        <p style={`font-family: var(--font-heading); font-size: 0.85rem; font-weight: 700; color: ${chapter.accent}; margin: 0 0 0.5rem;`}>Punto clave</p>
        <p style="font-size: 1rem; line-height: 1.75; margin: 0;">
          No es todo o nada. Los mejores flujos de trabajo con IA dan libertad en tareas de bajo riesgo y reversibles, mientras mantienen a los humanos en el ciclo para tareas de alto riesgo e irreversibles. Tu trabajo es diseñar esa frontera.
        </p>
      </div>
    </FadeInBlock>
  </section>

  <!-- ═══ WIDGET BRIDGE ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-4 pb-8">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] text-center mx-auto italic text-deep/50" style="max-width: 45ch; font-family: var(--font-body);">
        ¿Dónde trazarías la línea? Configura tus propios niveles de autonomía.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ THE WIDGET ═══ -->
  <section class="max-w-[900px] mx-auto px-6 md:px-10 py-8">
    <TrustThermometer client:visible />
  </section>

  <Divider accent={chapter.accent} client:load />

  <SharePrompt text="Aprendiendo sobre el uso de herramientas de IA y llamadas de funciones de la manera más práctica posible. Este curso te deja construir y probar cosas de verdad, no solo leer sobre ellas." client:visible />

  <!-- ═══ SEND-OFF ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-24">
    <FadeInBlock client:visible>
      <div class="pull-quote" style={`border-color: ${chapter.accent}; max-width: 50ch; margin: 0 auto 2rem;`}>
        Las herramientas convierten a la IA de asesora en asistente — de alguien que te dice qué hacer, a alguien que realmente lo hace.
      </div>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] text-center mx-auto" style="max-width: 55ch;">
        Le diste manos a la IA. Ahora conectamos esas herramientas en algo que puede perseguir un objetivo por su cuenta — planificando, ejecutando, recuperándose de errores. Es hora de construir agentes.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ BREAK ═══ -->
  <section id="break" class="py-16 px-6" style="background: linear-gradient(180deg, var(--color-cream) 0%, rgba(245,166,35,0.06) 15%, rgba(245,166,35,0.06) 85%, var(--color-cream) 100%);">
    <div class="max-w-[900px] mx-auto">
      <BreakReveal accentColor="#F5A623" toolName="Would You Let It?" client:visible>
        <WouldYouLetIt client:visible />
      </BreakReveal>
    </div>
  </section>

</ChapterLayout>
