---
export const prerender = true;
import ChapterLayout from '../../layouts/ChapterLayout.astro';
import { chapters } from '../../data/chapters';
import FadeInBlock from '../../components/scroll/FadeInBlock';
import AccentBar from '../../components/ui/AccentBar';
import Divider from '../../components/ui/Divider';
import FactOrFabrication from '../../components/widgets/ch9/FactOrFabrication';
import SweetTalker from '../../components/breaks/SweetTalker';
import BreakReveal from '../../components/breaks/BreakReveal';
import SharePrompt from '../../components/ui/SharePrompt';

const chapter = chapters[8];
---

<ChapterLayout chapter={chapter} locale="id">

  <!-- ═══ HERO ═══ -->
  <section class="min-h-[80vh] flex flex-col items-center justify-end px-6 pb-20 relative overflow-hidden">
    <div class="chapter-number" style={`color: ${chapter.accent};`}>9</div>

    <div class="max-w-[680px] mx-auto text-center relative z-10">
      <FadeInBlock client:visible>
        <p class="text-[13px] italic text-subtle/60 mb-5" style="font-family: var(--font-body);">
          Bab Sembilan
        </p>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.15}>
        <h1
          class="mb-8 leading-[1.08]"
          style={`font-family: var(--font-heading); color: ${chapter.accent}; font-size: clamp(2.8rem, 7vw, 4.5rem); font-weight: 800; letter-spacing: -0.03em;`}
        >
          Ketika AI<br />Salah
        </h1>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.3}>
        <p class="text-lg md:text-xl italic text-deep/40 leading-relaxed max-w-sm mx-auto" style="font-family: var(--font-body);">
          Kebiasaan verifikasi yang membedakan<br />amatir dari profesional.
        </p>
      </FadeInBlock>
    </div>

    <div class="absolute bottom-6 left-1/2 -translate-x-1/2 opacity-20 animate-pulse">
      <div class="w-px h-10 bg-deep/40"></div>
    </div>
  </section>

  <!-- ═══ OPENING HOOK ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-24 pb-16">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] drop-cap" style="margin: 0;">
        AI yang baru saja membantumu menulis essay sempurna juga bisa, dengan keyakinan yang sama, bilang bahwa Napoleon memenangkan Pertempuran Waterloo. Tidak ragu. Tidak gagap. Dia akan menyatakan "fakta" yang sepenuhnya dikarang dengan nada otoritatif yang persis sama untuk segala hal lainnya. Inilah paradoks di jantung bekerja dengan AI: <strong>kelancaran yang sama yang membuatnya berguna juga membuatnya berbahaya.</strong>
      </p>
    </FadeInBlock>
  </section>

  <Divider accent={chapter.accent} client:load />

  <!-- ═══ CONCEPT: HALLUCINATION ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-16">
    <FadeInBlock client:visible>
      <AccentBar color={chapter.accent} className="w-16 mb-10" client:visible />
      <h2 class="mb-8" style="font-family: var(--font-heading); letter-spacing: -0.02em;">
        Kewajaran Bukan Kebenaran
      </h2>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] mb-10" style="margin-top: 0;">
        Model AI tidak mengambil fakta dari database. Mereka memprediksi kata berikutnya yang paling mungkin. Ketika kamu bertanya tentang sesuatu yang nyata, data pelatihan menuntunnya ke informasi yang benar. Ketika kamu bertanya tentang sesuatu yang tidak ada, model tidak bilang "Aku tidak tahu." Dia menghasilkan apa yang <em>akan terlihat seperti</em> jawaban yang benar — lengkap dengan nama, tanggal, dan sitasi. Semuanya dikarang.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.2}>
      <p class="text-lg leading-[1.85] mb-14">
        Tidak ada korelasi antara seberapa yakin AI terdengar dan seberapa akurat jawabannya. Jawaban salah dan jawaban benar terlihat identik dari luar. Kamu tidak bisa menilai akurasi dari nada, dari detail, atau dari berapa banyak angka spesifik yang AI masukkan. Tes "kedengarannya benar" itu tidak berguna. Satu-satunya pertahananmu adalah verifikasi.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.3}>
      <div class="insight-box">
        <p style={`font-family: var(--font-heading); font-size: 0.85rem; font-weight: 700; color: ${chapter.accent}; margin: 0 0 0.5rem;`}>Poin penting</p>
        <p style="font-size: 1rem; line-height: 1.75; margin: 0;">
          AI tidak punya konsep kebenaran. AI punya konsep <em>kewajaran</em>. Dia menghasilkan teks yang cocok dengan pola — entah teks itu sesuai dengan kenyataan atau tidak.
        </p>
      </div>
    </FadeInBlock>
  </section>

  <!-- ═══ WIDGET BRIDGE ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-4 pb-8">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] text-center mx-auto italic text-deep/50" style="max-width: 45ch; font-family: var(--font-body);">
        Uji detektormu. Bisakah kamu membedakan klaim AI yang nyata dan yang dikarang?
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ THE WIDGET ═══ -->
  <section class="max-w-[900px] mx-auto px-6 md:px-10 py-8">
    <FactOrFabrication client:visible />
  </section>

  <Divider accent={chapter.accent} client:load />

  <SharePrompt text="Setiap pengguna AI butuh bab ini. Mengajarkan persis bagaimana AI berhalusinasi, kenapa dia setuju dengan jawaban yang salah, dan cara menangkapnya. Sangat praktis." client:visible />

  <!-- ═══ SEND-OFF ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-24">
    <FadeInBlock client:visible>
      <div class="pull-quote" style={`border-color: ${chapter.accent}; max-width: 50ch; margin: 0 auto 2rem;`}>
        Orang yang mendapat nilai paling besar dari AI adalah mereka yang sudah belajar persis kapan harus percaya dan kapan harus memverifikasi.
      </div>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] text-center mx-auto" style="max-width: 55ch;">
        Mengetahui kapan AI salah mengarah ke pertanyaan yang lebih besar: apa yang AI <em>tidak akan pernah</em> bisa lakukan? Selanjutnya, kita eksplorasi skill-skill yang tetap keras kepala, indahnya manusiawi — dan kenapa mereka lebih penting dari sebelumnya.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ BREAK ═══ -->
  <section id="break" class="py-16 px-6" style="background: linear-gradient(180deg, var(--color-cream) 0%, rgba(233,69,96,0.06) 15%, rgba(233,69,96,0.06) 85%, var(--color-cream) 100%);">
    <div class="max-w-[900px] mx-auto">
      <BreakReveal accentColor="#E94560" toolName="Sweet Talker" client:visible>
        <SweetTalker client:visible />
      </BreakReveal>
    </div>
  </section>

</ChapterLayout>
