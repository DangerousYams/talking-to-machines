---
export const prerender = true;
import ChapterLayout from '../../layouts/ChapterLayout.astro';
import { chapters } from '../../data/chapters';
import FadeInBlock from '../../components/scroll/FadeInBlock';
import AccentBar from '../../components/ui/AccentBar';
import Divider from '../../components/ui/Divider';
import ToolWall from '../../components/widgets/ch4/ToolWall';
import DreamProject from '../../components/breaks/DreamProject';
import BreakReveal from '../../components/breaks/BreakReveal';
import SharePrompt from '../../components/ui/SharePrompt';
import ForYouCard from '../../components/ui/ForYouCard';

const chapter = chapters[3];
const conceptsSummary = chapter.concepts.map(c => c.name).join(', ');
---

<ChapterLayout chapter={chapter} locale="de">

  <!-- ═══ HERO ═══ -->
  <section class="min-h-[80vh] flex flex-col items-center justify-end px-6 pb-20 relative overflow-hidden">
    <div class="chapter-number" style={`color: ${chapter.accent};`}>4</div>

    <div class="max-w-[680px] mx-auto text-center relative z-10">
      <FadeInBlock client:visible>
        <p class="text-[13px] italic text-subtle/60 mb-5" style="font-family: var(--font-body);">
          Kapitel Vier
        </p>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.15}>
        <h1
          class="mb-8 leading-[1.08]"
          style={`font-family: var(--font-heading); color: ${chapter.accent}; font-size: clamp(2.8rem, 7vw, 4.5rem); font-weight: 800; letter-spacing: -0.03em;`}
        >
          Die KI-<br />Landschaft
        </h1>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.3}>
        <p class="text-lg md:text-xl italic text-deep/40 leading-relaxed max-w-sm mx-auto" style="font-family: var(--font-body);">
          Ein Feldführer zu jedem KI-Tool,<br />das du gerade jetzt nutzen kannst.
        </p>
        <p class="mt-6" style="font-family: var(--font-mono); font-size: 0.7rem; letter-spacing: 0.08em; color: rgba(26,26,46,0.3);">
          ZULETZT AKTUALISIERT MÄRZ 2026
        </p>
      </FadeInBlock>
    </div>

    <div class="absolute bottom-6 left-1/2 -translate-x-1/2 opacity-20 animate-pulse">
      <div class="w-px h-10 bg-deep/40"></div>
    </div>
  </section>

  <!-- ═══ OPENING HOOK ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-24 pb-16">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] drop-cap" style="margin: 0;">
        Wenn die meisten Leute &bdquo;KI&ldquo; sagen, meinen sie ChatGPT. Das ist, als würde man &bdquo;das Internet&ldquo; sagen und Google meinen. Gerade jetzt gibt es Hunderte KI-Tools, die du heute nutzen kannst &mdash; Tools, die fotorealistische Bilder aus einem Satz generieren, ganze Songs komponieren oder jedes akademische Paper zu deinem Forschungsthema lesen und den Konsens in drei Absätzen zusammenfassen. Dieses Kapitel ist deine Landkarte des Terrains.
      </p>
    </FadeInBlock>
  </section>

  <Divider accent={chapter.accent} client:load />

  <!-- ═══ THE EIGHT FAMILIES ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-16">
    <FadeInBlock client:visible>
      <AccentBar color={chapter.accent} className="w-16 mb-10" client:visible />
      <h2 class="mb-8" style="font-family: var(--font-heading);">
        Die acht Familien
      </h2>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] mb-14" style="margin-top: 0;">
        Stell es dir wie Musikinstrumente vor. Ein Klavier und ein Schlagzeug sind beide &bdquo;Instrumente&ldquo;, aber du würdest keinen Pianisten bitten, einen Beat zu legen. KI-Tools sind genauso &mdash; ein Tool für Bildgenerierung ist grundlegend anders als eines für Code. Jedes Tool, dem du begegnest, gehört zu einer dieser acht Familien.
      </p>
    </FadeInBlock>

    <div style="display: flex; flex-direction: column; gap: 2rem; margin-bottom: 3rem;">
      {[
        { icon: '1', name: 'Bildgeneratoren', color: '#E94560', text: 'Text rein, Bilder raus. Midjourney, DALL-E 3, Stable Diffusion, Flux, Ideogram.' },
        { icon: '2', name: 'Bildbearbeitung', color: '#F5A623', text: 'Mit einem echten Bild starten und es transformieren. Photoshop AI, Magnific, Clipdrop.' },
        { icon: '3', name: 'Video-Erstellung', color: '#7B61FF', text: 'Videoclips aus Text oder Standbildern generieren. Sora, Runway, Kling, Pika.' },
        { icon: '4', name: 'Musik & Audio', color: '#16C79A', text: 'Ganze Songs per Prompt, Stimmklone, Soundeffekte. Suno, Udio, ElevenLabs.' },
        { icon: '5', name: 'Recherche-Agenten', color: '#0EA5E9', text: 'KI, die das Internet für dich liest. Perplexity, Elicit, Consensus, NotebookLM.' },
        { icon: '6', name: 'KI-Browser', color: '#6B7280', text: 'Agenten, die das Web wie ein Mensch navigieren. Arc, Operator, Claude Computer Use.' },
        { icon: '7', name: 'Coding-Tools', color: '#E94560', text: 'KI, die Code schreibt, bearbeitet und debuggt. Claude Code, Cursor, Antigravity, Copilot.' },
        { icon: '8', name: 'Chatbots & Assistenten', color: '#0F3460', text: 'Die universellen KI-Gesprächspartner, die du schon kennst: ChatGPT, Claude, Gemini.' },
      ].map((family, i) => (
        <FadeInBlock client:visible delay={i * 0.06}>
          <div style={`display: flex; gap: 1.5rem; align-items: flex-start;`}>
            <div style={`flex-shrink: 0; width: 40px; height: 40px; border-radius: 10px; display: flex; align-items: center; justify-content: center; font-family: var(--font-heading); font-size: 1.1rem; font-weight: 800; color: white; background: ${family.color};`}>
              {family.icon}
            </div>
            <div style="flex: 1;">
              <p style={`font-family: var(--font-heading); font-size: 1.1rem; font-weight: 700; color: ${family.color}; margin: 0 0 0.35rem; line-height: 1.3;`}>
                {family.name}
              </p>
              <p style="font-size: 1rem; line-height: 1.75; color: var(--color-deep); opacity: 0.75; margin: 0;">
                {family.text}
              </p>
            </div>
          </div>
        </FadeInBlock>
      ))}
    </div>

    <FadeInBlock client:visible delay={0.5}>
      <div class="insight-box">
        <p style={`font-family: var(--font-heading); font-size: 0.85rem; font-weight: 700; color: ${chapter.accent}; margin: 0 0 0.5rem;`}>Wichtige Erkenntnis</p>
        <p style="font-size: 1rem; line-height: 1.75; margin: 0;">
          Wer 10 KI-Tools oberflächlich kennt, wird dauerhaft besser abschneiden als jemand, der ein einziges Tool in der Tiefe kennt. Ein professioneller Handwerker hat kein Lieblingswerkzeug &mdash; er hat ein Lieblingswerkzeug <em>für jede Aufgabe</em>.
        </p>
      </div>
    </FadeInBlock>
  </section>

  <!-- ═══ WIDGET BRIDGE ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-4 pb-8">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] text-center mx-auto italic text-deep/50" style="font-family: var(--font-body); text-wrap: balance;">
        Entdecke den vollständigen Katalog. Klick auf ein Tool, um zu erfahren, was es kann.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ THE WIDGET ═══ -->
  <section class="max-w-[900px] mx-auto px-6 md:px-10 py-8">
    <ToolWall client:visible />
  </section>

  <Divider accent={chapter.accent} client:load />

  <div class="py-8">
    <ForYouCard
      chapterSlug={chapter.slug}
      chapterTitle={chapter.title}
      chapterConcepts={conceptsSummary}
      accentColor={chapter.accent}
      client:load
    />
  </div>
  <SharePrompt text="Es gibt viel mehr KI-Tools als mir bewusst war. Dieser interaktive Guide zeigt 8 Familien von Tools &mdash; Bildgenerierung, Video, Musik, Recherche, Coding &mdash; mit Beispielen für jede." client:visible />

  <!-- ═══ SEND-OFF ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-24">
    <FadeInBlock client:visible>
      <div class="pull-quote" style={`border-color: ${chapter.accent}; max-width: 50ch; margin: 0 auto 2rem;`}>
        Die Landschaft verändert sich jeden Monat. Aber die Kategorien sind stabil, auch wenn die Tools es nicht sind &mdash; und <em>das</em> ist die Karte, die es sich zu merken lohnt.
      </div>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] text-center mx-auto" style="max-width: 55ch;">
        Bisher reagieren diese Tools nur auf dich. Was wäre, wenn sie <em>selbstständig handeln</em> könnten? Das ist der Sprung vom Tool zum Agenten &mdash; und er verändert alles.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ BREAK ═══ -->
  <section id="break" class="py-16 px-6" style="background: linear-gradient(180deg, var(--color-cream) 0%, rgba(14,165,233,0.06) 15%, rgba(14,165,233,0.06) 85%, var(--color-cream) 100%);">
    <div class="max-w-[900px] mx-auto">
      <BreakReveal accentColor="#0EA5E9" toolName="Dream Project" client:visible>
        <DreamProject client:visible />
      </BreakReveal>
    </div>
  </section>

</ChapterLayout>
