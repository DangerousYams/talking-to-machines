---
export const prerender = true;
import ChapterLayout from '../../layouts/ChapterLayout.astro';
import { chapters } from '../../data/chapters';
import FadeInBlock from '../../components/scroll/FadeInBlock';
import AccentBar from '../../components/ui/AccentBar';
import Divider from '../../components/ui/Divider';
import StackDecoder from '../../components/widgets/ch8/StackDecoder';
import ComplexityScore from '../../components/breaks/ComplexityScore';
import BreakReveal from '../../components/breaks/BreakReveal';
import SharePrompt from '../../components/ui/SharePrompt';

const chapter = chapters[7];
---

<ChapterLayout chapter={chapter} locale="hi">

  <!-- ═══ HERO ═══ -->
  <section class="min-h-[80vh] flex flex-col items-center justify-end px-6 pb-20 relative overflow-hidden">
    <div class="chapter-number" style={`color: ${chapter.accent};`}>8</div>

    <div class="max-w-[680px] mx-auto text-center relative z-10">
      <FadeInBlock client:visible>
        <p class="text-[13px] italic text-subtle/60 mb-5" style="font-family: var(--font-body);">
          अध्याय आठ
        </p>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.15}>
        <h1
          class="mb-8 leading-[1.08]"
          style={`font-family: var(--font-heading); color: ${chapter.accent}; font-size: clamp(2.8rem, 7vw, 4.5rem); font-weight: 800; letter-spacing: -0.03em;`}
        >
          Complexity का<br />Orchestration
        </h1>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.3}>
        <p class="text-lg md:text-xl italic text-deep/40 leading-relaxed max-w-sm mx-auto" style="font-family: var(--font-body);">
          AI को manage करना projects के लिए<br />जो किसी भी context window से बड़े हैं।
        </p>
      </FadeInBlock>
    </div>

    <div class="absolute bottom-6 left-1/2 -translate-x-1/2 opacity-20 animate-pulse">
      <div class="w-px h-10 bg-deep/40"></div>
    </div>
  </section>

  <!-- ═══ OPENING HOOK ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-24 pb-16">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] drop-cap" style="margin: 0;">
        यही वो moment है जब हर ambitious beginner फंसता है: तुम AI से कुछ real बनाने को कहते हो — एक पूरा game, backend के साथ website, citations के साथ research paper — और सब कुछ बिखर जाता है। इसलिए नहीं कि AI काफी smart नहीं है। बल्कि इसलिए कि project एक single conversation के लिए बहुत बड़ा है। तुम requirements की wall paste करते हो और बीस minute बाद पता चलता है कि उसने आधा भूल दिया जो तुमने माँगा था।
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] mt-8">
        यह AI problem नहीं है। यह <strong>orchestration problem</strong> है। और इसे solve करना सीखना ही फर्क है एक person के बीच जो AI one-off tasks के लिए use करता है और जो इससे real things build करता है।
      </p>
    </FadeInBlock>
  </section>

  <Divider accent={chapter.accent} client:load />

  <!-- ═══ CONTEXT PACKING ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-16">
    <FadeInBlock client:visible>
      <AccentBar color={chapter.accent} className="w-16 mb-10" client:visible />
      <h2 class="mb-8" style="font-family: var(--font-heading);">
        Context Packing की Art
      </h2>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] mb-14" style="margin-top: 0;">
        Complex projects one-prompt approach को break कर देते हैं। Context overflow होता है, attention dilute होती है, errors compound होते हैं। Fix better prompt नहीं है — <strong>decomposition</strong> है। Project को focused tasks में break करो, फिर हर task के लिए carefully choose करो कि AI को actually कौन सा context चाहिए। बहुत कम दोगे तो hallucinate करेगा। बहुत ज्यादा दोगे तो focus खो देगा। Sweet spot surgical precision है।
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.2}>
      <p class="text-lg leading-[1.85] mb-14">
        इसे suitcase pack करने जैसा सोचो। तुम अपनी पूरी closet नहीं ले जा सकते। तुम्हें सोचना होगा कि कहाँ जा रहे हो (task), क्या चाहिए (relevant information), और क्या छोड़ सकते हो (बाकी सब कुछ)। हर conversation को exactly वो context मिलना चाहिए जो उसे चाहिए — उससे ज्यादा नहीं, उससे कम नहीं।
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.3}>
      <div class="insight-box">
        <p style="font-family: var(--font-heading); font-size: 0.85rem; font-weight: 700; color: var(--color-amber); margin: 0 0 0.5rem;">Key insight</p>
        <p style="font-size: 1rem; line-height: 1.75; margin: 0;">
          तुम्हारे handoff artifact की quality determine करती है downstream built होने वाली हर चीज़ की quality। Crisp schema vague description को हर बार beat करता है। Garbage in, garbage out — conversations के across।
        </p>
      </div>
    </FadeInBlock>
  </section>

  <!-- ═══ WIDGET BRIDGE ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-4 pb-8">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] text-center mx-auto italic text-deep/50" style="font-family: var(--font-body); text-wrap: balance;">
        Context suitcase pack करो। क्या तुम overflow के बिना जो matter करता है fit कर सकते हो?
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ THE WIDGET ═══ -->
  <section class="max-w-[900px] mx-auto px-6 md:px-10 py-8">
    <StackDecoder client:visible />
  </section>

  <Divider accent={chapter.accent} client:load />

  <SharePrompt text="AI का सबसे hard part prompting नहीं है — complex projects को multiple conversations में orchestrate करना है। इस course ने finally explain किया कैसे।" client:visible />

  <!-- ═══ SEND-OFF ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-24">
    <FadeInBlock client:visible>
      <div class="pull-quote" style="border-color: var(--color-navy); max-width: 50ch; margin: 0 auto 2rem;">
        Complex projects scary नहीं हैं। ये बस focused conversations की series हैं जो crisp artifacts से connected हैं।
      </div>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] text-center mx-auto" style="max-width: 55ch;">
        आगे: जानना कि AI कब <em>wrong</em> है। Hallucinations, sycophancy, और verification habits जो तुम्हें honest रखती हैं।
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ BREAK ═══ -->
  <section id="break" class="py-16 px-6" style="background: linear-gradient(180deg, var(--color-cream) 0%, rgba(15,52,96,0.06) 15%, rgba(15,52,96,0.06) 85%, var(--color-cream) 100%);">
    <div class="max-w-[900px] mx-auto">
      <BreakReveal accentColor="#0F3460" toolName="Complexity Score" client:visible>
        <ComplexityScore client:visible />
      </BreakReveal>
    </div>
  </section>

</ChapterLayout>
