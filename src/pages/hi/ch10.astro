---
export const prerender = true;
import ChapterLayout from '../../layouts/ChapterLayout.astro';
import { chapters } from '../../data/chapters';
import FadeInBlock from '../../components/scroll/FadeInBlock';
import AccentBar from '../../components/ui/AccentBar';
import Divider from '../../components/ui/Divider';
import TasteTest from '../../components/widgets/ch10/TasteTest';
import IrreplaceableYou from '../../components/breaks/IrreplaceableYou';
import BreakReveal from '../../components/breaks/BreakReveal';
import SharePrompt from '../../components/ui/SharePrompt';

const chapter = chapters[9];
---

<ChapterLayout chapter={chapter} locale="hi">

  <!-- ═══ HERO ═══ -->
  <section class="min-h-[80vh] flex flex-col items-center justify-end px-6 pb-20 relative overflow-hidden">
    <div class="chapter-number" style={`color: ${chapter.accent};`}>10</div>

    <div class="max-w-[680px] mx-auto text-center relative z-10">
      <FadeInBlock client:visible>
        <p class="text-[13px] italic text-subtle/60 mb-5" style="font-family: var(--font-body);">
          अध्याय दस
        </p>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.15}>
        <h1
          class="mb-8 leading-[1.08]"
          style={`font-family: var(--font-heading); color: ${chapter.accent}; font-size: clamp(2.8rem, 7vw, 4.5rem); font-weight: 800; letter-spacing: -0.03em;`}
        >
          इंसानी<br />Edge
        </h1>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.3}>
        <p class="text-lg md:text-xl italic text-deep/40 leading-relaxed max-w-sm mx-auto" style="font-family: var(--font-body);">
          जो AI की जगह नहीं ले सकता &mdash;<br />और जो 10x आसान बना देता है।
        </p>
      </FadeInBlock>
    </div>

    <div class="absolute bottom-6 left-1/2 -translate-x-1/2 opacity-20 animate-pulse">
      <div class="w-px h-10 bg-deep/40"></div>
    </div>
  </section>

  <!-- ═══ OPENING HOOK ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-24 pb-16">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] drop-cap" style="margin: 0;">
        जब भी कोई नई AI capability आती है, वही headline नज़र आती है: <em>"क्या AI [कोई job डालो] की जगह ले लेगा?"</em> यह गलत सवाल है। AI इंसानों की जगह नहीं लेता। यह <strong>tasks</strong> की जगह लेता है। और जब तुम किसी भी job को — किसी भी career, creative pursuit — closely देखते हो, तो एक mix मिलता है: कुछ tasks AI brilliantly handle करता है, कुछ में assist करता है, और कुछ stubbornly, beautifully human रहते हैं।
      </p>
    </FadeInBlock>
  </section>

  <Divider accent={chapter.accent} client:load />

  <!-- ═══ JOBS SHAPE-SHIFT ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-16">
    <FadeInBlock client:visible>
      <AccentBar color={chapter.accent} className="w-16 mb-10" client:visible />
      <h2 class="mb-8" style="font-family: var(--font-heading);">
        Jobs Disappear नहीं होतीं — Shape-Shift होती हैं
      </h2>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] mb-10" style="margin-top: 0;">
        जब ATMs आए, सबने predict किया कि bank tellers vanish हो जाएंगे। Instead, उनकी संख्या <em>बढ़ गई</em> — क्योंकि ATMs ने branches खोलना cheaper बना दिया, और teller की job cash count करने से relationships build करने में shift हो गई। AI वही कर रहा है, लेकिन faster और हर industry में एक साथ। Job title same रहता है। Job <em>description</em> dramatically change हो जाता है।
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.2}>
      <p class="text-lg leading-[1.85] mb-10">
        Data entry? AI handle कर लेता है। First drafts? Shockingly good। लेकिन <em>क्या build करना है choose करना</em>, <em>crisis में room read करना</em>, <em>decide करना कि data का क्या मतलब है</em> — इनके लिए judgment, empathy, और vision चाहिए जो AI के पास fundamentally नहीं है।
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.3}>
      <div class="insight-box">
        <p style={`font-family: var(--font-heading); font-size: 0.85rem; font-weight: 700; color: ${chapter.accent}; margin: 0 0 0.5rem;`}>Key insight</p>
        <p style="font-size: 1rem; line-height: 1.75; margin: 0;">
          यह "humans vs. AI" नहीं है। यह <strong>humans with AI vs. humans without AI</strong> है। जो लोग AI tools के साथ काम करना सीखते हैं वो अपनी jobs नहीं खोते — वो अपनी jobs faster, better, और उन capabilities के साथ करते हैं जो पहले नहीं थीं।
        </p>
      </div>
    </FadeInBlock>
  </section>

  <!-- ═══ WIDGET BRIDGE ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-4 pb-8">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] text-center mx-auto italic text-deep/50" style="font-family: var(--font-body); text-wrap: balance;">
        नीचे कोई career pick करो और देखो exactly कैसे AI इसे inside से transform करता है। एक भी task पूरी तरह disappear नहीं होता।
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ THE WIDGET ═══ -->
  <section class="max-w-[900px] mx-auto px-6 md:px-10 py-8">
    <TasteTest client:visible />
  </section>

  <Divider accent={chapter.accent} client:load />

  <SharePrompt text="यह course AI को hype नहीं करता और न ही jobs के बारे में fear-monger करता है। Exactly map करता है कौन सी skills human रहती हैं और कौन सी augmented होती हैं। Refreshingly honest।" client:visible />

  <!-- ═══ SEND-OFF ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-24">
    <FadeInBlock client:visible>
      <div class="pull-quote" style={`border-color: ${chapter.accent}; max-width: 50ch; margin: 0 auto 2rem;`}>
        AI options generate करता है। तुम सही वाला choose करते हो। वो judgment — taste, empathy, vision, first principles — तुम्हारा edge है।
      </div>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] text-center mx-auto" style="max-width: 55ch;">
        एक chapter बाकी है। अब कोई theory नहीं। Time है जो कुछ तुमने सीखा है उसे combine करने और कुछ real build करने का &mdash; कुछ ऐसा जो सिर्फ तुम बनाने के बारे में सोचोगे।
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ BREAK ═══ -->
  <section id="break" class="py-16 px-6" style="background: linear-gradient(180deg, var(--color-cream) 0%, rgba(22,199,154,0.06) 15%, rgba(22,199,154,0.06) 85%, var(--color-cream) 100%);">
    <div class="max-w-[900px] mx-auto">
      <BreakReveal accentColor="#16C79A" toolName="Irreplaceable You" client:visible>
        <IrreplaceableYou client:visible />
      </BreakReveal>
    </div>
  </section>

</ChapterLayout>
