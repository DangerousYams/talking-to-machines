---
export const prerender = true;
import ChapterLayout from '../../layouts/ChapterLayout.astro';
import { chapters } from '../../data/chapters';
import FadeInBlock from '../../components/scroll/FadeInBlock';
import AccentBar from '../../components/ui/AccentBar';
import Divider from '../../components/ui/Divider';
import ContextWindowViz from '../../components/widgets/ch3/ContextWindowViz';
import VibeCheck from '../../components/breaks/VibeCheck';
import BreakReveal from '../../components/breaks/BreakReveal';
import SharePrompt from '../../components/ui/SharePrompt';

const chapter = chapters[2];
---

<ChapterLayout chapter={chapter} locale="hi">

  <!-- ═══ HERO ═══ -->
  <section class="min-h-[80vh] flex flex-col items-center justify-end px-6 pb-20 relative overflow-hidden">
    <div class="chapter-number" style={`color: ${chapter.accent};`}>3</div>

    <div class="max-w-[680px] mx-auto text-center relative z-10">
      <FadeInBlock client:visible>
        <p class="text-[13px] italic text-subtle/60 mb-5" style="font-family: var(--font-body);">
          अध्याय तीन
        </p>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.15}>
        <h1
          class="mb-8 leading-[1.08]"
          style={`font-family: var(--font-heading); color: ${chapter.accent}; font-size: clamp(2.8rem, 7vw, 4.5rem); font-weight: 800; letter-spacing: -0.03em;`}
        >
          Context<br />Engineering
        </h1>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.3}>
        <p class="text-lg md:text-xl italic text-deep/40 leading-relaxed max-w-sm mx-auto" style="font-family: var(--font-body);">
          AI भूलता नहीं &mdash; वो पहले से<br />याद ही नहीं रख रहा था।
        </p>
      </FadeInBlock>
    </div>

    <div class="absolute bottom-6 left-1/2 -translate-x-1/2 opacity-20 animate-pulse">
      <div class="w-px h-10 bg-deep/40"></div>
    </div>
  </section>

  <!-- ═══ OPENING HOOK ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-24 pb-16">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] drop-cap" style="margin: 0;">
        हर बार जब तुम AI को message भेजते हो, वो तुम्हारी <em>पूरी conversation</em> scratch से पढ़ता है &mdash; हर message जो तुमने भेजी, हर response जो इसने दिया &mdash; reply generate करता है, और फिर सब कुछ भूल जाता है। अगली बार, same thing। पूरा transcript फिर से feed होता है। ये world का सबसे diligent लेकिन amnesiac reader है: हर बार completely thorough, हर turn पर zero से start।
      </p>
    </FadeInBlock>
  </section>

  <Divider accent={chapter.accent} client:load />

  <!-- ═══ CONCEPT SETUP ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-16">
    <FadeInBlock client:visible>
      <AccentBar color={chapter.accent} className="w-16 mb-10" client:visible />
      <h2 class="mb-8" style="font-family: var(--font-heading);">
        Window के Edges हैं
      </h2>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] mb-10" style="margin-top: 0;">
        वो transcript जो AI हर turn पर re-read करता है? वो <strong>context window</strong> नाम की चीज़ के अंदर रहता है &mdash; जो <strong>tokens</strong> में measure होती है (roughly एक token per three-quarters of a word)। Claude की window लगभग 200,000 tokens hold करती है &mdash; एक 500-page book। Huge लगता है, लेकिन तुम्हारी हर message, हर response, और हर hidden system instruction सब same budget से खाते हैं। तुम्हारे सोचे से fast भरती है।
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.15}>
      <p class="text-lg leading-[1.85] mb-10">
        जब budget खत्म हो जाता है, सबसे पुरानी messages edge से fall off हो जाती हैं। तुमने अपनी dietary restrictions message three में mention की थीं? Message fifteen तक AI शायद उन्हें देख ही न पाए। वो careless नहीं हो रहा &mdash; literally नहीं देख सकता जो अब उसके सामने नहीं है। AI सिर्फ उतना ही अच्छा है जितनी information currently उसकी window में है।
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.2}>
      <p class="text-lg leading-[1.85] mb-14">
        इसीलिए सबसे अच्छे AI users सिर्फ good prompts नहीं लिखते &mdash; वो manage करते हैं कि window में क्या <em>जाता है</em>। Periodically summarize करो ताकि key details visible रहें। जो matter करता है उसे front-load करो। Topic shift हो तो fresh start करो। Structured formats use करो ताकि AI context efficiently parse कर सके। <strong>Context engineering ही नई prompt engineering है।</strong>
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.25}>
      <div class="insight-box">
        <p style={`font-family: var(--font-heading); font-size: 0.85rem; font-weight: 700; color: ${chapter.accent}; margin: 0 0 0.5rem;`}>Key insight</p>
        <p style="font-size: 1rem; line-height: 1.75; margin: 0;">
          Great prompt लिखना step one है। Decide करना कि AI के सामने कौन सी information रखनी है &mdash; और कैसे structure करनी है &mdash; यही असली leverage है। World का best prompt भी fail हो जाता है अगर context wrong है।
        </p>
      </div>
    </FadeInBlock>
  </section>

  <!-- ═══ WIDGET BRIDGE ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-4 pb-8">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] text-center mx-auto italic text-deep/50" style="max-width: 45ch; font-family: var(--font-body);">
        Context window को भरते, overflow होते, और भूलते देखो &mdash; real time में।
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ THE WIDGET ═══ -->
  <section class="max-w-[900px] mx-auto px-6 md:px-10 py-8">
    <ContextWindowViz client:visible />
  </section>

  <Divider accent={chapter.accent} client:load />

  <SharePrompt text="Finally समझ आया कि AI conversation के बीच में चीज़ें क्यों 'भूल' जाता है। यह memory नहीं है — यह context window है। इस course ने live demos के साथ बहुत clearly explain किया।" client:visible />

  <!-- ═══ SEND-OFF ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-24">
    <FadeInBlock client:visible>
      <div class="pull-quote" style={`border-color: ${chapter.accent}; max-width: 50ch; margin: 0 auto 2rem;`}>
        अब तुम whiteboard देख सकते हो। Window के edges feel कर सकते हो। यह AI के साथ तुम्हारी हर conversation बदल देता है।
      </div>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] text-center mx-auto" style="max-width: 55ch;">
        अब तक हम chatbots के साथ काम कर रहे थे। आगे, हम zoom out करते हैं &mdash; chat window के beyond सैकड़ों AI tools exist करते हैं, और landscape जानना बदल देता है कि तुम क्या possible समझते हो।
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ BREAK ═══ -->
  <section id="break" class="py-16 px-6" style="background: linear-gradient(180deg, var(--color-cream) 0%, rgba(123,97,255,0.06) 15%, rgba(123,97,255,0.06) 85%, var(--color-cream) 100%);">
    <div class="max-w-[900px] mx-auto">
      <BreakReveal accentColor="#7B61FF" toolName="Vibe Check" client:visible>
        <VibeCheck client:visible />
      </BreakReveal>
    </div>
  </section>

</ChapterLayout>
