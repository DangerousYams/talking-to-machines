---
export const prerender = true;
import ChapterLayout from '../../layouts/ChapterLayout.astro';
import { chapters } from '../../data/chapters';
import FadeInBlock from '../../components/scroll/FadeInBlock';
import AccentBar from '../../components/ui/AccentBar';
import Divider from '../../components/ui/Divider';
import FlipTheScript from '../../components/widgets/ch2/FlipTheScript';
import SocraticSmackdown from '../../components/breaks/SocraticSmackdown';
import BreakReveal from '../../components/breaks/BreakReveal';
import SharePrompt from '../../components/ui/SharePrompt';

const chapter = chapters[1];
---

<ChapterLayout chapter={chapter} locale="ta">

  <!-- ═══ HERO ═══ -->
  <section class="min-h-[80vh] flex flex-col items-center justify-end px-6 pb-20 relative overflow-hidden">
    <div class="chapter-number" style={`color: ${chapter.accent};`}>2</div>

    <div class="max-w-[680px] mx-auto text-center relative z-10">
      <FadeInBlock client:visible>
        <p class="text-[13px] italic text-subtle/60 mb-5" style="font-family: var(--font-body);">
          அத்தியாயம் இரண்டு
        </p>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.15}>
        <h1
          class="mb-8 leading-[1.08]"
          style={`font-family: var(--font-heading); color: ${chapter.accent}; font-size: clamp(2.8rem, 7vw, 4.5rem); font-weight: 800; letter-spacing: -0.03em;`}
        >
          கேள்வி கேட்கும்<br />கலை
        </h1>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.3}>
        <p class="text-lg md:text-xl italic text-deep/40 leading-relaxed max-w-sm mx-auto" style="font-family: var(--font-body);">
          சிறந்த prompt என்பது AI யிடம்<br />கேள்விகள் கேட்க வைக்கும் கேள்வி.
        </p>
      </FadeInBlock>
    </div>

    <div class="absolute bottom-6 left-1/2 -translate-x-1/2 opacity-20 animate-pulse">
      <div class="w-px h-10 bg-deep/40"></div>
    </div>
  </section>

  <!-- ═══ OPENING HOOK ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-24 pb-16">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] drop-cap" style="margin: 0;">
        பெரும்பாலான மக்கள் AI யை vending machine போல் பயன்படுத்துகிறார்கள். Prompt போட்டு, பதில் வாங்கு. ஆனால் சிறந்த முடிவுகள் கட்டளைகளிலிருந்து அல்ல, <em>உரையாடல்களிலிருந்து</em> வருகின்றன. Prompt டைப் செய்பவனுக்கும் அதை craft செய்பவனுக்கும் உள்ள வித்தியாசம், 2,400 ஆண்டுகளாக சிறந்த சிந்தனையாளர்களை இயக்கும் அதே வித்தியாசம்: சரியான கேள்விகள் கேட்பது.
      </p>
    </FadeInBlock>
  </section>

  <Divider accent={chapter.accent} client:load />

  <!-- ═══ THE SOCRATIC FLIP ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-16">
    <FadeInBlock client:visible>
      <AccentBar color={chapter.accent} className="w-16 mb-10" client:visible />
      <h2 class="mb-8" style="font-family: var(--font-heading);">
        சாக்ரட்டீஸ் முறை திருப்பம்
      </h2>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] mb-14" style="margin-top: 0;">
        சாக்ரட்டீஸ் ஒருபோதும் கேள்வியை நேராக பதில் சொல்லவில்லை. அவர் <em>மேலும்</em> கேள்விகளை கேட்டார் — மாணவர்கள் ஆழமான புரிதலை தாங்களே அடையும் வரை. நீ AI யுடன் அதே அணுகுமுறையை பயன்படுத்தலாம், ஆனால் தலைகீழாக: பதில் கேட்பதற்கு பதிலாக, AI <em>உன்னை</em> interview செய்யச் சொல். நீ "பிறந்தநாள் விழா திட்டமிடு" என்று டைப் செய்யும்போது, பதிலை பெருமளவில் மாற்றும் டஜன் கணக்கான விவரங்களை விட்டுவிடுகிறாய். AI க்கு தெரியாது — ஆனால் அது கேட்கலாம்.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.2}>
      <p class="text-lg leading-[1.85] mb-14">
        இந்த ஒற்றை நுட்பம் — யார் கேள்விகள் கேட்கிறார்கள் என்பதை தலைகீழாக மாற்றுவது — AI யை பதில் இயந்திரத்திலிருந்து <strong>சிந்தனை பங்குதாரராக</strong> மாற்றுகிறது. Probe செய்யவும், clarify செய்யவும், challenge செய்யவும் அனுமதி கொடு. ஐந்து back-and-forth கேள்விகளுக்குப் பிறகு வரும் output, ஒரு single-shot command-உடன் ஒப்பிட்டால் அடையாளம் கண்டுகொள்ள முடியாத அளவு மாறியிருக்கும்.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.3}>
      <div class="insight-box">
        <p style={`font-family: var(--font-heading); font-size: 0.85rem; font-weight: 700; color: ${chapter.accent}; margin: 0 0 0.5rem;`}>முக்கிய கருத்து</p>
        <p style="font-size: 1rem; line-height: 1.75; margin: 0;">
          சிறந்த AI பயனர்கள் அந்த விஷயத்தில் நிபுணர்கள் அல்ல — அவர்கள் கேள்வி கேட்பதில் நிபுணர்கள். நீ கேட்கக்கூடிய மிக சக்திவாய்ந்த கேள்வி: "பதில் சொல்வதற்கு முன், உனக்கு என்ன தெரிய வேண்டும்?"
        </p>
      </div>
    </FadeInBlock>
  </section>

  <!-- ═══ WIDGET BRIDGE ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-4 pb-8">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] text-center mx-auto italic text-deep/50" style="max-width: 45ch; font-family: var(--font-body);">
        நீயே முயற்சித்துப் பார். ஒரு இலக்கு சொல் — AI script-ஐ எப்படி புரட்டுகிறது என்று பார்.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ THE WIDGET ═══ -->
  <section id="flip-the-script" class="max-w-[900px] mx-auto px-6 md:px-10 py-8">
    <FlipTheScript client:visible />
  </section>

  <Divider accent={chapter.accent} client:load />

  <SharePrompt text="This AI course just flipped how I think about prompting. Instead of telling AI what to do, you ask it to interview YOU first. The results are night and day." client:visible />

  <!-- ═══ SEND-OFF ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-24">
    <FadeInBlock client:visible>
      <div class="pull-quote" style={`border-color: ${chapter.accent}; max-width: 50ch; margin: 0 auto 2rem;`}>
        AI க்கு என்ன நினைக்க வேண்டும் என்று சொல்வதை நிறுத்து. உன்னுடன் சேர்ந்து சிந்திக்க இடம் கொடு.
      </div>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] text-center mx-auto" style="max-width: 55ch;">
        ஆனால் நீ கற்றுக்கொண்ட ஒவ்வொரு நுட்பமும் அதே கண்ணுக்கு தெரியாத சுவரில் மோதுகிறது: <em>context window</em>. அடுத்தது, அதை திறந்து AI உண்மையில் என்ன பார்க்கிறது என்பதை engineer செய்ய கற்றுக்கொள்கிறோம்.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ BREAK ═══ -->
  <section id="break" class="py-16 px-6" style="background: linear-gradient(180deg, var(--color-cream) 0%, rgba(22,199,154,0.06) 15%, rgba(22,199,154,0.06) 85%, var(--color-cream) 100%);">
    <div class="max-w-[900px] mx-auto">
      <BreakReveal accentColor="#16C79A" toolName="Socratic Smackdown" client:visible>
        <SocraticSmackdown client:visible />
      </BreakReveal>
    </div>
  </section>

</ChapterLayout>
