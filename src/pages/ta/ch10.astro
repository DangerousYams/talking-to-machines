---
export const prerender = true;
import ChapterLayout from '../../layouts/ChapterLayout.astro';
import { chapters } from '../../data/chapters';
import FadeInBlock from '../../components/scroll/FadeInBlock';
import AccentBar from '../../components/ui/AccentBar';
import Divider from '../../components/ui/Divider';
import TasteTest from '../../components/widgets/ch10/TasteTest';
import IrreplaceableYou from '../../components/breaks/IrreplaceableYou';
import BreakReveal from '../../components/breaks/BreakReveal';
import SharePrompt from '../../components/ui/SharePrompt';

const chapter = chapters[9];
---

<ChapterLayout chapter={chapter} locale="ta">

  <!-- ═══ HERO ═══ -->
  <section class="min-h-[80vh] flex flex-col items-center justify-end px-6 pb-20 relative overflow-hidden">
    <div class="chapter-number" style={`color: ${chapter.accent};`}>10</div>

    <div class="max-w-[680px] mx-auto text-center relative z-10">
      <FadeInBlock client:visible>
        <p class="text-[13px] italic text-subtle/60 mb-5" style="font-family: var(--font-body);">
          அத்தியாயம் பத்து
        </p>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.15}>
        <h1
          class="mb-8 leading-[1.08]"
          style={`font-family: var(--font-heading); color: ${chapter.accent}; font-size: clamp(2.8rem, 7vw, 4.5rem); font-weight: 800; letter-spacing: -0.03em;`}
        >
          மனித<br />முன்னணி
        </h1>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.3}>
        <p class="text-lg md:text-xl italic text-deep/40 leading-relaxed max-w-sm mx-auto" style="font-family: var(--font-body);">
          AI replace செய்ய முடியாதது &mdash;<br />10x easy ஆக்குவது.
        </p>
      </FadeInBlock>
    </div>

    <div class="absolute bottom-6 left-1/2 -translate-x-1/2 opacity-20 animate-pulse">
      <div class="w-px h-10 bg-deep/40"></div>
    </div>
  </section>

  <!-- ═══ OPENING HOOK ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-24 pb-16">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] drop-cap" style="margin: 0;">
        ஒவ்வொரு முறை புதிய AI capability வரும்போது, அதே headline தோன்றும்: <em>"AI [insert job]-ஐ replace செய்துவிடுமா?"</em> தவறான கேள்வி. AI மனிதர்களை replace செய்வதில்லை. <strong>Tasks-ஐ</strong> replace செய்கிறது. எந்த job-ஐயும் &mdash; career, creative pursuit &mdash; zoom in செய்து பார்த்தால், ஒரு mix காண்பாய்: சில tasks-ஐ AI brilliantly handle செய்கிறது, சிலவற்றில் assist செய்கிறது, சில பிடிவாதமாக, அழகாக மனிதனாகவே இருக்கின்றன.
      </p>
    </FadeInBlock>
  </section>

  <Divider accent={chapter.accent} client:load />

  <!-- ═══ JOBS SHAPE-SHIFT ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-16">
    <FadeInBlock client:visible>
      <AccentBar color={chapter.accent} className="w-16 mb-10" client:visible />
      <h2 class="mb-8" style="font-family: var(--font-heading);">
        வேலைகள் மறையாது &mdash; உருமாறும்
      </h2>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] mb-10" style="margin-top: 0;">
        ATMs வந்தபோது, bank tellers மறைந்துவிடுவார்கள் என்று எல்லாரும் predict செய்தார்கள். அதற்கு பதிலாக, எண்ணிக்கை <em>அதிகரித்தது</em> &mdash; ATMs புதிய branches திறப்பதை cheaper ஆக்கியது, teller-ன் வேலை cash எண்ணுவதிலிருந்து relationships build செய்வதற்கு shift ஆனது. AI அதே விஷயம் செய்கிறது, ஆனால் faster-ஆகவும் ஒவ்வொரு industry-லும் ஒரே நேரத்தில்.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.2}>
      <p class="text-lg leading-[1.85] mb-10">
        Data entry? AI handle செய்கிறது. First drafts? Shockingly good. ஆனால் <em>என்ன build செய்ய வேண்டும் என்று choose செய்வது</em>, <em>crisis-ல் room read செய்வது</em>, <em>data என்ன mean ஆகிறது என்று decide செய்வது</em> &mdash; இவற்றுக்கு judgment, empathy, vision தேவை &mdash; AI கிட்ட fundamentally இல்லாதவை.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.3}>
      <div class="insight-box">
        <p style={`font-family: var(--font-heading); font-size: 0.85rem; font-weight: 700; color: ${chapter.accent}; margin: 0 0 0.5rem;`}>முக்கிய கருத்து</p>
        <p style="font-size: 1rem; line-height: 1.75; margin: 0;">
          இது "மனிதர்கள் vs. AI" அல்ல. <strong>AI உடன் மனிதர்கள் vs. AI இல்லாத மனிதர்கள்</strong>. AI tools-உடன் வேலை செய்ய கற்றுக்கொள்பவர்கள் வேலை இழப்பதில்லை &mdash; வேகமாகவும், better-ஆகவும், முன்பு இல்லாத capabilities-உடனும் வேலை செய்கிறார்கள்.
        </p>
      </div>
    </FadeInBlock>
  </section>

  <!-- ═══ WIDGET BRIDGE ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-4 pb-8">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] text-center mx-auto italic text-deep/50" style="font-family: var(--font-body); text-wrap: balance;">
        கீழே ஒரு career select செய்து AI அதை உள்ளிருந்து எப்படி transform செய்கிறது என்று பார். ஒரு single task கூட முழுமையாக மறையாது.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ THE WIDGET ═══ -->
  <section class="max-w-[900px] mx-auto px-6 md:px-10 py-8">
    <TasteTest client:visible />
  </section>

  <Divider accent={chapter.accent} client:load />

  <SharePrompt text="This course doesn't hype AI or fear-monger about jobs. It maps exactly which skills stay human and which get augmented. Refreshingly honest." client:visible />

  <!-- ═══ SEND-OFF ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-24">
    <FadeInBlock client:visible>
      <div class="pull-quote" style={`border-color: ${chapter.accent}; max-width: 50ch; margin: 0 auto 2rem;`}>
        AI options generate செய்கிறது. நீ சரியானதை choose செய்கிறாய். அந்த judgment &mdash; taste, empathy, vision, first principles &mdash; அதுதான் உன் edge.
      </div>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] text-center mx-auto" style="max-width: 55ch;">
        ஒரே ஒரு chapter மிச்சம். இனி theory இல்லை. நீ கற்றுக்கொண்ட எல்லாவற்றையும் combine செய்து உண்மையான ஒன்று build செய்யும் நேரம் &mdash; நீ மட்டுமே நினைக்கும் ஒன்று.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ BREAK ═══ -->
  <section id="break" class="py-16 px-6" style="background: linear-gradient(180deg, var(--color-cream) 0%, rgba(22,199,154,0.06) 15%, rgba(22,199,154,0.06) 85%, var(--color-cream) 100%);">
    <div class="max-w-[900px] mx-auto">
      <BreakReveal accentColor="#16C79A" toolName="Irreplaceable You" client:visible>
        <IrreplaceableYou client:visible />
      </BreakReveal>
    </div>
  </section>

</ChapterLayout>
