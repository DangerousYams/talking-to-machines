---
export const prerender = true;
import ChapterLayout from '../../layouts/ChapterLayout.astro';
import { chapters } from '../../data/chapters';
import FadeInBlock from '../../components/scroll/FadeInBlock';
import AccentBar from '../../components/ui/AccentBar';

import Divider from '../../components/ui/Divider';
import ProjectPlanner from '../../components/widgets/ch11/ProjectPlanner';
import ShowcaseGallery from '../../components/widgets/ch11/ShowcaseGallery';

const chapter = chapters[10];
---

<ChapterLayout chapter={chapter} locale="mr">

  <!-- ═══ HERO ═══ -->
  <section class="min-h-[80vh] flex flex-col items-center justify-end px-6 pb-20 relative overflow-hidden">
    <!-- Giant background number -->
    <div class="chapter-number" style={`color: ${chapter.accent};`}>11</div>

    <div class="max-w-[680px] mx-auto text-center relative z-10">
      <FadeInBlock client:visible>
        <p class="text-[13px] italic text-subtle/60 mb-5" style="font-family: var(--font-body);">
          धडा अकरा
        </p>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.15}>
        <h1
          class="mb-8 leading-[1.08]"
          style={`font-family: var(--font-heading); color: ${chapter.accent}; font-size: clamp(2.8rem, 7vw, 4.5rem); font-weight: 800; letter-spacing: -0.03em;`}
        >
          खरंच काहीतरी<br />बनव
        </h1>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.3}>
        <p class="text-lg md:text-xl italic text-deep/40 leading-relaxed max-w-sm mx-auto" style="font-family: var(--font-body);">
          Creation शिवाय ज्ञान म्हणजे trivia.<br />Ship करायची वेळ आली.
        </p>
      </FadeInBlock>
    </div>

    <!-- Scroll cue -->
    <div class="absolute bottom-6 left-1/2 -translate-x-1/2 opacity-20 animate-pulse">
      <div class="w-px h-10 bg-deep/40"></div>
    </div>
  </section>

  <!-- ═══ STORY OPENER — with drop cap ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-24 pb-16">
    <FadeInBlock client:visible>
      <p class="drop-cap text-lg leading-[1.85] mb-8">
        तू शेवटच्या धड्यापर्यंत पोहोचलास. मागच्या दहा धड्यांमध्ये तू "help me with my essay" पासून agents बनवणं, hallucinations debug करणं, आणि work च्या future बद्दल विचार करणं इथपर्यंत आलास. AI बद्दल तू बहुतेक adults पेक्षा जास्त शिकलास. पण एक uncomfortable सत्य आहे: <strong>तू काहीतरी बनवेपर्यंत काहीच महत्त्वाचं नाही.</strong>
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] mb-8">
        AI बद्दल वाचणं म्हणजे swimming बद्दल वाचण्यासारखं आहे. तू buoyancy चं physics study करू शकतोस, Olympic races बघू शकतोस, प्रत्येक stroke technique पाठ करू शकतोस. पण पाण्यात उतरेपर्यंत, तुला त्यातलं काहीच <em>माहीत</em> नाही. तुला फक्त <em>त्याबद्दल</em> माहीत आहे.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.2}>
      <p class="text-lg leading-[1.85]">
        हा धडा उडी मारण्याबद्दल आहे. Toy exercise किंवा follow-along tutorial नाही. एक खरा project. तू निवडलेला. तुला ज्याची काळजी आहे. कुणाला दाखवून सांगता येईल असं: <em>"मी हे बनवलं."</em>
      </p>
    </FadeInBlock>
  </section>

  <Divider accent={chapter.accent} client:load />

  <!-- ═══ YOUR TOOLKIT ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-16">
    <FadeInBlock client:visible>
      <AccentBar color={chapter.accent} className="w-16 mb-10" client:visible />
      <h2 class="mb-8" style="font-family: var(--font-heading); letter-spacing: -0.02em;">
        तुझं Toolkit
      </h2>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] mb-10">
        दहा धड्यांच्या skills, वापरायला तयार:
      </p>
    </FadeInBlock>

    <!-- Compact 2-column grid of chapter skills -->
    <FadeInBlock client:visible delay={0.15}>
      <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 0.5rem 1.5rem; margin-bottom: 2.5rem;">
        {[
          { ch: '1', label: 'Prompt architecture', color: '#E94560' },
          { ch: '2', label: 'Advanced techniques', color: '#0F3460' },
          { ch: '3', label: 'Context engineering', color: '#7B61FF' },
          { ch: '4', label: 'AI landscape', color: '#0EA5E9' },
          { ch: '5', label: 'Tool use & agents', color: '#F5A623' },
          { ch: '6', label: 'Agent architecture', color: '#E94560' },
          { ch: '7', label: 'AI pair programming', color: '#7B61FF' },
          { ch: '8', label: 'Orchestration', color: '#0F3460' },
          { ch: '9', label: 'Critical thinking', color: '#E94560' },
          { ch: '10', label: 'The human edge', color: '#16C79A' },
        ].map((item) => (
          <div style="display: flex; align-items: center; gap: 0.6rem; padding: 0.4rem 0;">
            <span style={`font-family: var(--font-mono); font-size: 0.6rem; font-weight: 700; color: ${item.color}; opacity: 0.7; width: 1.25rem; text-align: right; flex-shrink: 0;`}>{item.ch}</span>
            <span style={`font-family: var(--font-body); font-size: 0.9rem; color: var(--color-deep); opacity: 0.8;`}>{item.label}</span>
          </div>
        ))}
      </div>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.25}>
      <p class="text-lg leading-[1.85]">
        आता प्रश्न हा आहे: यातून तू काय करशील?
      </p>
    </FadeInBlock>
  </section>

  <Divider accent={chapter.accent} client:load />

  <!-- ═══ PLAN BEFORE YOU BUILD ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-16">
    <FadeInBlock client:visible>
      <AccentBar color={chapter.accent} className="w-16 mb-10" client:visible />
      <h2 class="mb-8" style="font-family: var(--font-heading);">
        बनवण्याआधी Plan कर
      </h2>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] mb-10">
        पहिल्यांदा बनवणारे लोक सर्वात मोठी चूक कुठली करतात? ते सरळ tools कडे जातात. Claude उघडतात, एक मोठा prompt टाइप करतात, आणि एका शॉट मध्ये सगळं बनवायचा प्रयत्न करतात. Fail होतं. Frustration येतो. ते सोडून देतात.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.15}>
      <div class="pull-quote" style={`border-color: ${chapter.accent};`}>
        सर्वोत्तम projects prompt ने सुरू होत नाहीत. कागदावरच्या plan ने सुरू होतात. काय बनवतोयस? कुणासाठी? "Done" कसं दिसतं? Keyboard ला हात लावण्याआधी ही उत्तरं दे.
      </div>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.2}>
      <p class="text-lg leading-[1.85] mb-10">
        धडा 8 आठव — complexity orchestrate करणं? तुझा capstone project त्याची miniature version आहे. काम तुकड्यांमध्ये मोड. काय बनवतोयस ते बनवण्याआधी माहीत असू दे. प्रत्येक तुकड्यावर AI ला collaborator म्हणून वापर, संपूर्ण गोष्टीसाठी magic wand म्हणून नाही.
      </p>
    </FadeInBlock>

    <!-- Planning Steps -->
    <FadeInBlock client:visible delay={0.25}>
      <div style={`position: relative; padding-left: 2.5rem; margin-bottom: 3rem;`}>
        <div style={`position: absolute; left: 7px; top: 8px; bottom: 8px; width: 1px; background: linear-gradient(to bottom, ${chapter.accent}, ${chapter.accent}20);`}></div>
        {[
          { word: 'निवड.', desc: 'तुला खरोखर ज्याची काळजी आहे ते निवड. Passion कठीण भागांमध्ये persistence fuel करतं.' },
          { word: 'Scope ठरव.', desc: 'अर्ध्यावर कट कर. मग पुन्हा अर्ध्यावर कट कर. पूर्ण झालेला छोटा project अर्धवट सोडलेल्या मोठ्या project पेक्षा चांगला.' },
          { word: 'Decompose कर.', desc: '5-8 tasks मध्ये मोड. प्रत्येक task एका बसण्यात पूर्ण होता कामा.' },
          { word: 'बनव.', desc: 'एक वेळी एक task. प्रत्येक तुकड्यासाठी AI वापर. पुढे जाण्याआधी प्रत्येक output verify कर.' },
          { word: 'Ship कर.', desc: 'Done perfect पेक्षा चांगलं. Share कर. Feedback घे. Iterate कर.' },
        ].map((step, i) => (
          <div style={`position: relative; padding-bottom: ${i < 4 ? '1.75rem' : '0'};`}>
            <div style={`position: absolute; left: -2.5rem; top: 6px; width: 15px; height: 15px; border-radius: 50%; border: 2px solid ${chapter.accent}; background: var(--color-cream);`}></div>
            <p style="margin: 0; font-size: 1rem; line-height: 1.5;">
              <strong style={`color: ${chapter.accent}; font-family: var(--font-heading); font-weight: 700;`}>{step.word}</strong>
              <span style="opacity: 0.7;"> {step.desc}</span>
            </p>
          </div>
        ))}
      </div>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.35}>
      <p class="text-lg leading-[1.85]">
        एक गोष्ट आणखी: जसं बनव तसं document कर. Project स्वतःच अर्धी story आहे. ते कसं बनवलंस — decisions, iterations, dead ends — ही दुसरी अर्धी. Explain करता येणारा पूर्ण झालेला project कुठल्याही test score पेक्षा जास्त valuable आहे.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ WIDGET: PROJECT PLANNER ═══ -->
  <section class="max-w-[900px] mx-auto px-6 md:px-10 py-8">
    <FadeInBlock client:visible>
      <ProjectPlanner client:visible />
    </FadeInBlock>
  </section>

  <Divider accent={chapter.accent} client:load />

  <!-- ═══ THE 3-WEEK SPRINT ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-16">
    <FadeInBlock client:visible>
      <AccentBar color={chapter.accent} className="w-16 mb-10" client:visible />
      <h2 class="mb-8" style="font-family: var(--font-heading);">
        3-आठवड्यांचा Sprint
      </h2>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] mb-10">
        तुझा project weekend चा असो किंवा semester चा, rhythm तोच आहे. खरोखर काहीतरी ship करण्यासाठी हा battle-tested framework:
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.15}>
      <div style="display: flex; flex-direction: column; gap: 2rem;">
        {[
          {
            week: 'आठवडा 1',
            title: 'Ideate आणि Scope',
            color: '#F5A623',
            tasks: [
              'तुझा project निवड. वर Project Planner वापर.',
              '"Done" कसं दिसतं याचं एक paragraph लिहा.',
              'Project 5-8 tasks मध्ये मोड, clear deliverables सह.',
              'प्रत्येक task साठी कुठली AI tools वापरशील ते ओळख.',
              'सर्वात कठीण भागाचा tiny prototype आधी बनव. ते काम करत असेल तर बाकीचं follow करेल.',
            ],
          },
          {
            week: 'आठवडा 2',
            title: 'बनव',
            color: chapter.accent,
            tasks: [
              'एका वेळी एक task कर. Skip करू नकोस.',
              'प्रत्येक task साठी, धडा 1 मधला prompt-evaluate-refine loop वापर.',
              '"काम केलेले prompts" चा log ठेव. नंतर स्वतःला thank करशील.',
              'अडकलास तर relevant धडा पुन्हा वाच. उत्तर सहसा एखाद्या technique मध्ये असतं जी तू try केली नाहीस.',
              'मध्या-आठवड्यात मित्र किंवा कुटुंबातल्या कुणाकडून feedback घे. Fresh डोळे तुझ्या चुकल्या गोष्टी पकडतात.',
            ],
          },
          {
            week: 'आठवडा 3',
            title: 'Polish आणि Present',
            color: '#7B61FF',
            tasks: [
              'सर्वात obvious तीन rough edges fix कर. बाकीचं ignore कर.',
              'Project description लिहा: काय करतं, कसं बनवलं, काय शिकलं.',
              'तुझ्या process साठी critical असलेले 3 key prompts document कर.',
              '2-मिनिटांचा walkthrough बनव (video, slides, किंवा live demo).',
              'Share कर. Teacher सोबत, मित्रांसोबत, online, student showcase मध्ये.',
            ],
          },
        ].map((week, i) => (
          <FadeInBlock client:visible delay={0.15 + i * 0.1}>
            <div style={`background: ${week.color}06; border: 1px solid ${week.color}15; border-radius: 12px; padding: 1.5rem 1.75rem;`}>
              <div style="display: flex; align-items: center; gap: 10px; margin-bottom: 1rem;">
                <span style={`font-family: var(--font-mono); font-size: 0.65rem; font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase; color: ${week.color}; background: ${week.color}15; padding: 3px 10px; border-radius: 100;`}>
                  {week.week}
                </span>
                <span style={`font-family: var(--font-heading); font-size: 1.05rem; font-weight: 700; color: var(--color-deep);`}>
                  {week.title}
                </span>
              </div>
              <ul style="margin: 0; padding-left: 1.25rem;">
                {week.tasks.map((task) => (
                  <li style="font-family: var(--font-body); font-size: 0.9rem; line-height: 1.7; color: var(--color-deep); opacity: 0.8; margin-bottom: 0.35rem;">
                    {task}
                  </li>
                ))}
              </ul>
            </div>
          </FadeInBlock>
        ))}
      </div>
    </FadeInBlock>
  </section>

  <Divider accent={chapter.accent} client:load />

  <!-- BRIDGE TO SHOWCASE GALLERY -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-4 pb-8">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] text-center mx-auto italic text-deep/50" style="max-width: 45ch; font-family: var(--font-body);">
        दुसऱ्या students नी काय बनवलं ते बघ. मग तुझं gallery मध्ये add कर.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ WIDGET: SHOWCASE GALLERY ═══ -->
  <section class="max-w-[900px] mx-auto px-6 md:px-10 py-8">
    <FadeInBlock client:visible>
      <ShowcaseGallery client:visible />
    </FadeInBlock>
  </section>

  <Divider accent={chapter.accent} client:load />

  <!-- ═══ CLOSING — THE SEND-OFF ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-24">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] text-center mx-auto mb-8" style="max-width: 55ch;">
        अकरा धडे आधी, तू "help me with my essay" टाइप केलंस आणि best ची आशा धरलीस. आता तू context engineer करतोस, agents design करतोस, outputs verify करतोस, आणि प्रत्येक step वर judgment आणतोस.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] text-center mx-auto mb-8" style="max-width: 55ch;">
        Tools गोष्टी बनवत नाहीत. <strong>Tools असलेले लोक गोष्टी बनवतात.</strong> आणि तुझ्याकडे तुझ्या generation ने कधी access केलेलं सर्वोत्तम toolkit आहे.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.2}>
      <p class="text-lg leading-[1.85] text-center mx-auto" style="max-width: 55ch;">
        जा आणि फक्त तुलाच सुचेल असं काहीतरी बनव.
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.3}>
      <p class="text-center mt-8" style={`font-family: var(--font-heading); font-size: 1.8rem; font-weight: 800; color: ${chapter.accent}; letter-spacing: -0.02em;`}>
        Ship कर.
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ COURSE COMPLETE ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pb-24">
    <FadeInBlock client:visible>
      <div style={`text-align: center; padding: 3rem 2rem; border-radius: 16px; background: ${chapter.accent}06; border: 1px solid ${chapter.accent}15; position: relative; overflow: hidden;`}>
        <!-- Decorative corner accents -->
        <div style={`position: absolute; top: -1px; left: -1px; width: 40px; height: 40px; border-top: 2px solid ${chapter.accent}40; border-left: 2px solid ${chapter.accent}40; border-radius: 16px 0 0 0;`}></div>
        <div style={`position: absolute; bottom: -1px; right: -1px; width: 40px; height: 40px; border-bottom: 2px solid ${chapter.accent}40; border-right: 2px solid ${chapter.accent}40; border-radius: 0 0 16px 0;`}></div>

        <p style="font-family: var(--font-mono); font-size: 0.6rem; font-weight: 700; letter-spacing: 0.15em; text-transform: uppercase; color: var(--color-subtle); margin: 0 0 0.75rem;">
          अभ्यासक्रम पूर्ण
        </p>
        <p style={`font-family: var(--font-heading); font-size: 1.5rem; font-weight: 800; color: ${chapter.accent}; margin: 0 0 0.75rem; letter-spacing: -0.02em;`}>
          तू सगळे 11 धडे पूर्ण केलेस.
        </p>
        <p style="font-family: var(--font-body); font-size: 0.95rem; line-height: 1.7; color: var(--color-deep); opacity: 0.6; max-width: 40ch; margin: 0 auto 2rem;">
          आता खरं शिकणं सुरू होतं — जा आणि काहीतरी बनव, आणि गरज लागली तेव्हा कुठल्याही धड्याकडे परत ये.
        </p>

        <div style="display: flex; flex-wrap: wrap; justify-content: center; gap: 0.75rem;">
          <a
            href="/"
            style={`display: inline-flex; align-items: center; gap: 6px; padding: 0.6rem 1.5rem; border-radius: 100px; font-family: var(--font-body); font-size: 0.85rem; font-weight: 600; text-decoration: none; background: ${chapter.accent}; color: white; transition: all 0.3s;`}
          >
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6" /></svg>
            मुख्यपृष्ठावर परत
          </a>
          <a
            href="/mr/ch1"
            style="display: inline-flex; align-items: center; gap: 6px; padding: 0.6rem 1.5rem; border-radius: 100px; font-family: var(--font-body); font-size: 0.85rem; font-weight: 600; text-decoration: none; border: 1px solid rgba(26,26,46,0.12); color: var(--color-deep); transition: all 0.3s;"
          >
            धडा 1 पुन्हा वाच
          </a>
        </div>
      </div>
    </FadeInBlock>
  </section>

</ChapterLayout>
