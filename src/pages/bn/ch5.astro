---
export const prerender = true;
import ChapterLayout from '../../layouts/ChapterLayout.astro';
import { chapters } from '../../data/chapters';
import FadeInBlock from '../../components/scroll/FadeInBlock';
import AccentBar from '../../components/ui/AccentBar';
import Divider from '../../components/ui/Divider';
import TrustThermometer from '../../components/widgets/ch5/TrustThermometer';
import WouldYouLetIt from '../../components/breaks/WouldYouLetIt';
import BreakReveal from '../../components/breaks/BreakReveal';
import SharePrompt from '../../components/ui/SharePrompt';
import ForYouCard from '../../components/ui/ForYouCard';

const chapter = chapters[4];
const conceptsSummary = chapter.concepts.map(c => c.name).join(', ');
---

<ChapterLayout chapter={chapter} locale="bn">

  <!-- ═══ HERO ═══ -->
  <section class="min-h-[80vh] flex flex-col items-center justify-end px-6 pb-20 relative overflow-hidden">
    <div class="chapter-number" style={`color: ${chapter.accent};`}>৫</div>

    <div class="max-w-[680px] mx-auto text-center relative z-10">
      <FadeInBlock client:visible>
        <p class="text-[13px] italic text-subtle/60 mb-5" style="font-family: var(--font-body);">
          পঞ্চম অধ্যায়
        </p>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.15}>
        <h1
          class="mb-8 leading-[1.08]"
          style={`font-family: var(--font-heading); color: ${chapter.accent}; font-size: clamp(2.8rem, 7vw, 4.5rem); font-weight: 800; letter-spacing: -0.03em;`}
        >
          ওকে দাও<br />টুল
        </h1>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.3}>
        <p class="text-lg md:text-xl italic text-deep/40 leading-relaxed max-w-sm mx-auto" style="font-family: var(--font-body);">
          "প্রশ্নের উত্তর দাও" থেকে<br />"কাজ করো"-তে লাফ।
        </p>
      </FadeInBlock>
    </div>

    <div class="absolute bottom-6 left-1/2 -translate-x-1/2 opacity-20 animate-pulse">
      <div class="w-px h-10 bg-deep/40"></div>
    </div>
  </section>

  <!-- ═══ OPENING HOOK ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-24 pb-16">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] drop-cap" style="margin: 0; --drop-cap-color: #F5A623;">
        এখন পর্যন্ত তোমার প্রতিটা AI ইন্টার‌্যাকশন একই প্যাটার্ন মেনে চলেছে। তুমি কিছু টাইপ করো। AI কিছু লিখে ফেরত দেয়। অত্যন্ত অত্যাধুনিক একটা অটোকমপ্লিট। কিন্তু যদি AI <em>কিছু করতে</em> পারতো? শুধু আবহাওয়ার কথা বলা না, বরং পূর্বাভাস চেক করা। শুধু কোড লেখা না, বরং চালানো। শুধু Google-এ কী সার্চ করতে হবে বলা না, বরং নিজেই ওয়েব সার্চ করে ফলাফল নিয়ে আসা।
      </p>
    </FadeInBlock>
  </section>

  <Divider accent={chapter.accent} client:load />

  <!-- ═══ THE TRUST QUESTION ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-16">
    <FadeInBlock client:visible>
      <AccentBar color={chapter.accent} className="w-16 mb-10" client:visible />
      <h2 class="mb-8" style="font-family: var(--font-heading);">
        বিশ্বাসের প্রশ্ন
      </h2>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] mb-14" style="margin-top: 0;">
        যখন তুমি AI-কে টুল দাও, তুমি ওকে একটা টুলবক্স ধরিয়ে বলো: <em>"এগুলো তুমি করতে পারো। কখন ব্যবহার করবে সেটা তুমি ঠিক করো।"</em> সে ওয়েব সার্চ করতে পারে, কোড চালাতে পারে, ফাইল পড়তে পারে, API কল করতে পারে। AI বেশি চালাক হয়নি — সে হয়েছে <strong>সংযুক্ত</strong>। আর এটা এমন একটা প্রশ্ন তোলে যেটার সাথে প্রযুক্তির কোনো সম্পর্ক নেই, সম্পর্ক আছে তোমার সাথে: তোমার সাথে চেক করার আগে AI কতটুকু নিজে থেকে করবে?
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.15}>
      <p class="text-lg leading-[1.85] mb-14">
        উত্তরটা তিনটা জিনিসের ওপর নির্ভর করে। <strong>ঝুঁকি</strong> — সবচেয়ে খারাপ কী হতে পারে? <strong>উল্টানো যায় কি না</strong> — পূর্বাবস্থায় ফেরানো যাবে? আর <strong>বিশ্বাস</strong> — এই টুলটাকে তুমি কতটা ভালো চেনো? শতবার ব্যবহার করা একটা ক্যালকুলেটর কখনো পরীক্ষা করোনি এমন নতুন একটা প্লাগইনের চেয়ে বেশি স্বাধীনতা পাওয়ার যোগ্য।
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.2}>
      <div class="insight-box">
        <p style={`font-family: var(--font-heading); font-size: 0.85rem; font-weight: 700; color: ${chapter.accent}; margin: 0 0 0.5rem;`}>মূল শিক্ষা</p>
        <p style="font-size: 1rem; line-height: 1.75; margin: 0;">
          এটা সব-অথবা-কিছুই না। সেরা AI ওয়ার্কফ্লোগুলো কম-ঝুঁকি, উল্টানো যায় এমন কাজে স্বাধীনতা দেয়, আর বেশি-ঝুঁকি, অপরিবর্তনীয় কাজে মানুষকে লুপে রাখে। সীমানা ডিজাইন করাটাই তোমার কাজ।
        </p>
      </div>
    </FadeInBlock>
  </section>

  <!-- ═══ WIDGET BRIDGE ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-4 pb-8">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] text-center mx-auto italic text-deep/50" style="font-family: var(--font-body); text-wrap: balance;">
        তুমি কোথায় সীমারেখা টানবে? নিজের স্বায়ত্তশাসন লেভেল ঠিক করো।
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ THE WIDGET ═══ -->
  <section class="max-w-[900px] mx-auto px-6 md:px-10 py-8">
    <TrustThermometer client:visible />
  </section>

  <Divider accent={chapter.accent} client:load />

  <div class="py-8">
    <ForYouCard
      chapterSlug={chapter.slug}
      chapterTitle={chapter.title}
      chapterConcepts={conceptsSummary}
      accentColor={chapter.accent}
      client:load
    />
  </div>
  <SharePrompt text="AI টুল ইউজ আর ফাংশন কলিং সম্পর্কে সবচেয়ে হ্যান্ডস-অন উপায়ে শিখছি। এই কোর্সে আসলেই জিনিস বানানো আর টেস্ট করা যায়, শুধু পড়া না।" client:visible />

  <!-- ═══ SEND-OFF ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-24">
    <FadeInBlock client:visible>
      <div class="pull-quote" style={`border-color: ${chapter.accent}; max-width: 50ch; margin: 0 auto 2rem;`}>
        টুল AI-কে উপদেষ্টা থেকে সহকারীতে বদলে দেয় — যে তোমাকে বলে কী করতে হবে, তার থেকে যে আসলেই করে দেয়।
      </div>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] text-center mx-auto" style="max-width: 55ch;">
        তুমি AI-কে হাত দিয়েছো। এরপর, আমরা সেই টুলগুলো এমন কিছুতে জুড়ে দেবো যেটা নিজে থেকে একটা লক্ষ্য অনুসরণ করতে পারে — পরিকল্পনা করে, কাজ করে, ভুল থেকে সামলে ওঠে। এবার এজেন্ট বানানোর পালা।
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ BREAK ═══ -->
  <section id="break" class="py-16 px-6" style="background: linear-gradient(180deg, var(--color-cream) 0%, rgba(245,166,35,0.06) 15%, rgba(245,166,35,0.06) 85%, var(--color-cream) 100%);">
    <div class="max-w-[900px] mx-auto">
      <BreakReveal accentColor="#F5A623" toolName="Would You Let It?" client:visible>
        <WouldYouLetIt client:visible />
      </BreakReveal>
    </div>
  </section>

</ChapterLayout>
