---
export const prerender = true;
import ChapterLayout from '../../layouts/ChapterLayout.astro';
import { chapters } from '../../data/chapters';
import FadeInBlock from '../../components/scroll/FadeInBlock';
import AccentBar from '../../components/ui/AccentBar';
import Divider from '../../components/ui/Divider';
import FlipTheScript from '../../components/widgets/ch2/FlipTheScript';
import SocraticSmackdown from '../../components/breaks/SocraticSmackdown';
import BreakReveal from '../../components/breaks/BreakReveal';
import SharePrompt from '../../components/ui/SharePrompt';

const chapter = chapters[1];
---

<ChapterLayout chapter={chapter} locale="bn">

  <!-- ═══ HERO ═══ -->
  <section class="min-h-[80vh] flex flex-col items-center justify-end px-6 pb-20 relative overflow-hidden">
    <div class="chapter-number" style={`color: ${chapter.accent};`}>২</div>

    <div class="max-w-[680px] mx-auto text-center relative z-10">
      <FadeInBlock client:visible>
        <p class="text-[13px] italic text-subtle/60 mb-5" style="font-family: var(--font-body);">
          দ্বিতীয় অধ্যায়
        </p>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.15}>
        <h1
          class="mb-8 leading-[1.08]"
          style={`font-family: var(--font-heading); color: ${chapter.accent}; font-size: clamp(2.8rem, 7vw, 4.5rem); font-weight: 800; letter-spacing: -0.03em;`}
        >
          জিজ্ঞেস করার<br />শিল্প
        </h1>
      </FadeInBlock>

      <FadeInBlock client:visible delay={0.3}>
        <p class="text-lg md:text-xl italic text-deep/40 leading-relaxed max-w-sm mx-auto" style="font-family: var(--font-body);">
          সেরা প্রম্পট হলো সেই প্রশ্ন যেটা<br />AI-কে তোমাকে প্রশ্ন করতে বাধ্য করে।
        </p>
      </FadeInBlock>
    </div>

    <div class="absolute bottom-6 left-1/2 -translate-x-1/2 opacity-20 animate-pulse">
      <div class="w-px h-10 bg-deep/40"></div>
    </div>
  </section>

  <!-- ═══ OPENING HOOK ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-24 pb-16">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] drop-cap" style="margin: 0;">
        বেশিরভাগ মানুষ AI-কে ভেন্ডিং মেশিনের মতো ব্যবহার করে। প্রম্পট ঢোকাও, উত্তর বেরোয়। কিন্তু সেরা ফলাফল আসে <em>কথোপকথন</em> থেকে, কমান্ড থেকে না। যে পার্থক্য একজন প্রম্পট টাইপ করা মানুষ আর একজন প্রম্পট ক্র্যাফট করা মানুষের মধ্যে — সেটা সেই একই পার্থক্য যা ২,৪০০ বছর ধরে মহান চিন্তাবিদদের শক্তি জুগিয়ে আসছে: সঠিক প্রশ্ন জিজ্ঞেস করা।
      </p>
    </FadeInBlock>
  </section>

  <Divider accent={chapter.accent} client:load />

  <!-- ═══ THE SOCRATIC FLIP ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-16">
    <FadeInBlock client:visible>
      <AccentBar color={chapter.accent} className="w-16 mb-10" client:visible />
      <h2 class="mb-8" style="font-family: var(--font-heading);">
        সক্রেটিক ফ্লিপ
      </h2>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] mb-14" style="margin-top: 0;">
        সক্রেটিস কখনো সরাসরি উত্তর দিতেন না। তিনি <em>আরো</em> প্রশ্ন করতেন যতক্ষণ না তাঁর ছাত্ররা নিজেরাই গভীর উপলব্ধিতে পৌঁছাত। তুমি AI-এর সাথে একই কৌশল ব্যবহার করতে পারো, কিন্তু উল্টো করে: উত্তর চাওয়ার বদলে, AI-কে বলো তোমাকে <em>ইন্টারভিউ</em> করতে। তুমি যখন টাইপ করো "একটা জন্মদিনের পার্টি প্ল্যান করো," তুমি এমন ডজনখানেক ডিটেইল বাদ দিচ্ছ যা উত্তরটাকে আমূল বদলে দেয়। AI জানে না — কিন্তু সে জিজ্ঞেস করতে পারে।
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.2}>
      <p class="text-lg leading-[1.85] mb-14">
        এই একটা টেকনিক — কে প্রশ্ন করবে সেটা উল্টে দেওয়া — AI-কে উত্তর দেওয়ার মেশিন থেকে একজন <strong>চিন্তার সঙ্গী</strong>-তে রূপান্তরিত করে। তাকে খোঁচা দেওয়ার, স্পষ্ট করার, আর চ্যালেঞ্জ করার অনুমতি দাও। পাঁচটা আদান-প্রদানের পর আউটপুট, একটা সিঙ্গেল-শট কমান্ডের তুলনায় চেনাই যাবে না।
      </p>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.3}>
      <div class="insight-box">
        <p style={`font-family: var(--font-heading); font-size: 0.85rem; font-weight: 700; color: ${chapter.accent}; margin: 0 0 0.5rem;`}>মূল বিষয়</p>
        <p style="font-size: 1rem; line-height: 1.75; margin: 0;">
          সেরা AI ব্যবহারকারীরা বিষয়ের এক্সপার্ট না — তারা জিজ্ঞেস করার এক্সপার্ট। আর সবচেয়ে শক্তিশালী প্রশ্ন যা তুমি করতে পারো সেটা হলো: "উত্তর দেওয়ার আগে, তোমার কি জানা দরকার?"
        </p>
      </div>
    </FadeInBlock>
  </section>

  <!-- ═══ WIDGET BRIDGE ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 pt-4 pb-8">
    <FadeInBlock client:visible>
      <p class="text-lg leading-[1.85] text-center mx-auto italic text-deep/50" style="max-width: 45ch; font-family: var(--font-body);">
        নিজে চেষ্টা করো। একটা লক্ষ্য বলো — আর দেখো AI কিভাবে স্ক্রিপ্ট উল্টে দেয়।
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ THE WIDGET ═══ -->
  <section id="flip-the-script" class="max-w-[900px] mx-auto px-6 md:px-10 py-8">
    <FlipTheScript client:visible />
  </section>

  <Divider accent={chapter.accent} client:load />

  <SharePrompt text="এই AI কোর্সটা আমার প্রম্পটিং নিয়ে চিন্তার ধরনই বদলে দিল। AI-কে কি করতে হবে বলার বদলে, তুমি তাকে বলো আগে তোমাকে ইন্টারভিউ করতে। ফলাফল রাত-দিন।" client:visible />

  <!-- ═══ SEND-OFF ═══ -->
  <section class="max-w-[680px] mx-auto px-6 md:px-10 py-24">
    <FadeInBlock client:visible>
      <div class="pull-quote" style={`border-color: ${chapter.accent}; max-width: 50ch; margin: 0 auto 2rem;`}>
        AI-কে কি ভাবতে হবে বলা বন্ধ করো। তাকে তোমার সাথে ভাবার জায়গা দাও।
      </div>
    </FadeInBlock>

    <FadeInBlock client:visible delay={0.1}>
      <p class="text-lg leading-[1.85] text-center mx-auto" style="max-width: 55ch;">
        কিন্তু তুমি যতগুলো টেকনিক শিখেছ সবগুলো একই অদৃশ্য দেয়ালে গিয়ে ঠেকে: <em>কনটেক্সট উইন্ডো</em>। পরের পর্বে, আমরা সেটা ভেঙে খুলব আর শিখব AI আসলে কি দেখে সেটা কিভাবে ইঞ্জিনিয়ার করতে হয়।
      </p>
    </FadeInBlock>
  </section>

  <!-- ═══ BREAK ═══ -->
  <section id="break" class="py-16 px-6" style="background: linear-gradient(180deg, var(--color-cream) 0%, rgba(22,199,154,0.06) 15%, rgba(22,199,154,0.06) 85%, var(--color-cream) 100%);">
    <div class="max-w-[900px] mx-auto">
      <BreakReveal accentColor="#16C79A" toolName="Socratic Smackdown" client:visible>
        <SocraticSmackdown client:visible />
      </BreakReveal>
    </div>
  </section>

</ChapterLayout>
